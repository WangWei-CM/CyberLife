# Cyberlife（赛博人生记录网站）— 项目策划书

> 版本：v2.1 | 2026-09-03（定名 Cyberlife + 新增 5.13 登录动画）
> 全部需求确认 + UI 详细设计

---

## 1. 产品概述

一款个人化的人生记录与规划平台，将"一段人生"实例化为一个数据空间，集日记、心情追踪、身体记录、任务/日程管理、里程碑、阶段目标与评论于一体。三屏时间导航（过去｜现在｜未来），每种状态有独立视觉风格，用户可自定义主题色。

---

## 2. 核心功能清单

### 2.1 现在页（Now）
- 顶部横幅：进行中规划轮播（组件，现在页风格，间隔可配置默认 6 秒）
- 左上角：日期、星期、农历、北京时间精确到秒（秒字体更小）
- 身体卡片（左=身体曲线，右=身体记录添加选项）
- 心情卡片（左=心情曲线，右=心情记录添加选项）
- 左下角：当日日记（MD 编辑器，默认可编辑）
- 右栏：日历/日程组件（现成开源项目）

### 2.2 过去页（Past）
- **可缩放人生时间轴**：范围=注册日→今天；滚轮缩放（向上放大至天级，向下缩小至月/年级，最大时屏幕内保持 3~5 天）、左键拖动平移、点击精确选中某一天
- 时间轴默认左侧，可拖放到上/下/右边缘（书写者专属布局，可保存）；竖置今天在底部，横置今天在右端
- 两个折线统计图组件（心情曲线+身体指数曲线）随时间轴范围联动，仅显示折线不标数值
- 心情曲线跨天聚合：一天多个点密集显示，横轴=天数，点按实际时间定位（如第 4.1/4.2/4.3 天）
- 心情曲线 Y 轴：按当前范围 min/max 自适应，不标数值（2026-09-03 确认）
- 选中日期后：**日记 与 该日日程（任务+里程碑+评论）左右对半开**
- 绝密🔒条目仅书写者可见；里程碑在时间轴上特殊标注（金色勋章节点），里程碑标志本身可设权限
- 评论系统

### 2.3 未来页（Future）
- **长期规划**：开始日期+截止日期+名称；默认展示日期范围内的规划；同时展示时间进度与计划进度；某天可标记计划进度（百分比）；可上传封面图、图标、简介、多个计划文件
- **布局（游戏卡片风格）**：上下两部分。上部左右 3:7（左=规划列表，右=规划详情）；下部=待办日历视图
- **待办日历**：自研缩放日历（可缩放粒度超出 FullCalendar 能力）。点击某天可在该日添加待办（优先级等沿用现成日历软件逻辑）；默认接下来 7 天视图，滚轮向下依次收缩到 14天/30天/半年/1年/5年/10年/30年/100年（最小7天最大100年）
- **阶段目标与长期规划合并**（2026-09-03 确认）：统一为一个功能——有起止日期、名称、简介、封面/图标/文件、进度（时间进度+计划进度）
- 进行中的规划在现在页横幅轮播展示（组件，现在页风格），横幅只展示进度不可编辑

### 2.4 通用功能
- **背景音乐歌单**（2026-09-03 确认）：三页各有独立歌单，进入页面轮播该页歌曲；系统内置默认音频+用户可上传；标题栏一个"开始/停止播放"按钮（三页共用）；切页换列表、切回断点续播（每页记忆曲目+进度）
- 主题模式三档（2026-09-03 确认）：**始终黑色 / 始终白色 / 日出日落**（按时间自动切换）；每页独立调色保留
- 设置：统一设置页（tab 分块：歌单/密钥/备份等）；心情/身体设置以卡片旁按钮弹窗展示，不进新页面
- 密钥管理（书写者创建/作废/补发/限时/备注密钥）
- 导出备份（按人生，输出到服务器配置路径，用户FTP自取）
- 失联/停止：7天无书写者登录→失联横幅；30天→停止（现在/未来页全员变灰）；有登录自动撤销+手动可撤
- **待办提醒**（2026-09-03 推翻早期"不做"）：沿用现成日历/待办应用逻辑（每待办可设提醒时间、可重复），经 APK 系统通知（仅安卓）
- **站内短信（通知中心）**（2026-09-03 新增）：触发源=新评论、密钥过期、规划到期、失联/停止状态变化、绝密解除；顶栏铃铛+红点未读、时间倒序、点击跳转、全部保留
- 无语音输入需求（输入法负责）——浏览器端不内置语音；语音转任务由 APK 承担（见 5.16）

---

## 3. 用户角色与权限模型

### 3.1 角色
| 角色 | 身份 | 说明 |
|------|------|------|
| 系统管理员 | /admin 独立密码登录 | 查看/增删书写者、随时复制主密钥、备份/导入指定书写者（2026-09-03 确认） |
| 人生书写者 | 主密钥登录（无用户名密码） | 一人一生，唯一写权限；密钥随机长串，由管理员创建并颁发，含书写者昵称（问候语显示）；丢失找管理员重发 |
| 阅读密钥持有者 | 只读密钥登录 | 不可写，按权限规则读取+评论；密钥同样随机生成 |

### 3.2 权限体系（2026-09-03 第三批确认）

**分层**：
1. **密钥级规则（时间锚点）**：密钥创建时刻，之前不可看之后永久可看。**对所有内容类型生效**——心情/身体记录不设条目级权限，密钥能否访问某天的心情/身体，唯一决定因素是那天日期是否在锚点允许范围内（整日不可访问）。
2. **条目级（仅日记/日程）**：每条日记/日程创建时右下角有权限按钮，可选用**权限预设**（如"仅家人可见"=指定若干 key 可见/不可见）。**锚点是最低门槛：预设只能在锚点允许范围内缩小可见 key 集合，不能扩大到锚点之前**。不选预设=对锚点范围内所有 key 可见（默认全公开）。
3. **权限预设**：由书写者定义（可见 key 集合）；**创建新 key 时手动确认此 key 在每个预设中的权限**（可见/不可见）。
4. **附件权限**（2026-09-03 确认）：一切附件（图片/文件）均**独立设置权限**；创建日记或目标时，其文件默认继承所在日记/目标的权限，可单独调整。
5. **绝密**（最高优先）：逐条标记，仅书写者可见，拒绝一切其他密钥。书写者可随时解除。心情/身体记录同样支持绝密标记。

**检查链**：绝密 → 条目级（日记/日程）→ 密钥级锚点。命中任一拦截则不可见。

### 3.3 绝密平行方案
- 同一天可并存：对外内容（公开层）+ 绝密内容（仅自己层）
- 日记：每天可有一篇对外日记 + 一篇绝密内心OS
- 心情：可记公开心情 + 绝密心情（绝密不计入对外曲线，但计入书写者全量曲线）
- 书写者默认只看到公开内容；开启**绝密模式**后编辑/查看绝密层

### 3.4 评论权限
- 创建内容时可勾选"可评论"
- 开启后：有权限看到该内容的人均可评论、回复
- 书写者可删任意评论，评论者可删自己的

---

## 4. 数据模型概要

### 4.1 实体
- **Writer**（书写者）：id, nickname, master_key_hash, life_id, created_at, 状态（主密钥登录，无用户名；昵称显示于问候语）
- **ReaderKey**（阅读密钥）：见下
- **Admin**（系统管理员）：password_hash（单管理员）
- **ReaderKey**（阅读密钥）：id, life_id, key_hash, nickname, avatar_path, 锚点(创建时刻), preset_permsJSON(每个预设的可见/不可见), 创建时间, 过期时间, 备注, 是否作废
- **PermPreset**（权限预设）：id, life_id, name(如"仅家人可见"), visible_key_idsJSON, invisible_key_idsJSON
- **Life**（人生）：id, owner_id, status(active/lost/stopped), last_active_at（无名称，仅概念）
- **DiaryEntry**（日记）：id, life_id, date, created_at, content_md, 附件JSON, 权限JSON, 绝密标志, 是否里程碑, 可评论标志
- **MoodRecord**（心情记录）：id, life_id, timestamp, tagsJSON, value(均值), 权限, 绝密
- **BodyRecord**（身体记录）：id, life_id, date, score(0-100), note, 权限, 绝密
- **Task**（任务/日程）：id, life_id, date, title, description, done, 权限, 绝密, 是否里程碑, 可评论标志
- **Milestone**（里程碑标记）：关联日记/task，加标志+权限，含 description(描述) + detail(详细信息)，展示时只显示描述
- **Plan**（长期规划，含阶段目标）：id, life_id, name, start_date, end_date, intro_md, cover_path, icon_path, filesJSON(每文件含独立权限), 权限, 绝密, 可评论标志
- **PlanProgress**（规划进度记录）：id, plan_id, date, percent(0-100)
- **Playlist**（页面歌单）：id, life_id, page(now/past/future), mode(list_loop/shuffle/single), tracksJSON, 排序
- **Track**（音频）：id, playlist_id, path, title, artist, duration, is_default, 排序
- **Comment**（评论）：id, target_type, target_id, author_key_id, content, created_at, deleted
- **InboxMessage**（站内短信）：id, life_id, type(comment/key_expired/plan_due/life_status/secret_opened), ref_id, text, read, created_at
- **MoodTag**（情绪标签）：id, life_id, emoji, text, value, 排序
- **BodyTag**（身体指标定义）：id, life_id, name, unit, 排序

### 4.2 数据文件结构
```
data/
├── global.db               # 全局：User、ReaderKey、Life
└── lives/
    └── {life_id}/
        ├── settings.json   # 人生配置（主题色、组件布局等）
        ├── 2026-09.db      # 月度数据
        ├── 2026-10.db
        └── …
uploads/
└── {life_id}/
    ├── avatars/            # 阅读者头像
    └── diary/              # 日记附件
        └── 2026-09/…
```

---

## 5. UI/UX 详细设计

### 5.1 全局框架

**导航栏**（顶栏）：
- 中央：**"过去·现在·未来"三段指示器**，当前屏高亮，可点击直达
- 指示器两侧：**左右翻屏箭头**（← →），不再在屏幕两侧
- 右侧：**背景音乐播放/停止按钮**（三页共用，2026-09-03）、亮暗切换、设置入口（tab 分块设置页）、当前身份头像昵称、**绝密模式开关**（书写者专属）
- 设置入口：进入统一设置页（tab：歌单 / 密钥管理 / **权限预设** / 主题 / 备份导出 / 登出）；心情/身体相关设置通过三页卡片旁的设置按钮弹窗，不进新页面
- 权限预设 tab：书写者增删改预设（命名 + 指定 key 可见/不可见）（2026-09-03 确认）
- 登出：头像菜单 + 设置页 两处入口（2026-09-03 确认）
- 阅读者设置页仅显示：音乐（音量/停止）+ 亮暗/主题；密钥/备份/预设/音频上传均不显示（2026-09-03 确认）
- 左侧：人生名称

**顶栏可拖拽**：用户可将顶栏拖到左/右/下边缘（通过设置或拖拽手柄）。

**整体布局**：两栏密铺为基础结构，不可改。各栏内的组件可拖拽调整位置。

### 5.2 现在页布局

**左栏**（从上到下）：
1. **时间区**：日期 + 星期 + 农历 + 北京时间（秒字体更小）
2. **身体卡片**（左右两半）：左=身体曲线折线图（不标数值），右=身体记录添加面板（滑杆0-100+文字框+提交）
3. **心情卡片**（左右两半）：左=心情曲线折线图（不标数值），右=心情添加面板（tag多选网格+备注+提交）
4. **日记区**：MD 编辑器，默认可编辑，自动存草稿+实时保存。删除超过50字时自动备份被删段落。编辑器全系统复用。

**右栏**：日历/日程组件（现成开源项目，如 FullCalendar），**日历即待办**——展示当日任务（2026-09-03 确认：左栏不单独放待办清单，当日待办并入右栏日历今日视图）。

### 5.3 绝密模式（书写者专用）

- 右上角状态栏的**按钮**，点击后整个主题色变红
- 日记编辑器切换为绝密内容编辑器（内容为绝密层，与公开层互不干扰）
- 公共编辑器自动存草稿为公开层，绝密模式存为绝密层
- 心情/身体记录自动带上绝密标签
- 再次点击退出绝密模式，回到公开层
- 绝密模式下的所有编辑器同样具备 自动存草稿、实时保存、删除备份 功能

### 5.4 过去页布局（2026-09-03 更新：可缩放时间轴模型）

```
过去页
├─ 时间轴（默认左侧，可拖放到 上/下/右，书写者专属布局可保存）
│   · 范围：注册日 → 今天（不含更早与未来）
│   · 滚轮：向上=放大（天级），向下=缩小（月→年级），最大时屏幕内 3~5 天
│   · 左键拖动=平移滚动；点击任意位置=精确选中某一天（刻度未显示也精确）
│   · 方向：竖置今天在底部；横置今天在右端
│   · 里程碑=金色勋章节点特殊标注
│   · 默认选中今天（最底部）
└─ 主区域
    · 顶部：心情曲线 + 身体指数曲线（两折线组件，随范围联动，不标数值）
    · 中部：选中日 日记 | 该日日程（任务+里程碑+评论） 左右对半开
    · 绝密🔒条目仅书写者可见，读者视图中完全消失
```
- 曲线图仅显示折线趋势，不标注具体数值

### 5.5 未来页布局（2026-09-03 更新）

**整体**：游戏卡片风格，上下两部分，分隔线可拖拽调整比例（2026-09-03 确认）。
- **上部**：左右 3:7，分隔线可拖拽调整比例。左=规划列表（按截止日期升序）；右=规划详情卡片（名称/起止日期/封面/图标/简介/文件列表/双进度条+进度标记入口）
- **编辑模式**：规划详情右上角"编辑模式"按钮，点击进入编辑——编辑 MD 简介、填写完成百分比（2026-09-03 确认）
- **下部**：自研可缩放待办日历。点击某天=该日添加待办（沿用现成日历软件优先级等逻辑）；默认接下来 7 天；滚轮向下收缩 14/30/半年/1年/5年/10年/30年/100年（最小7天最大100年）；过去的任务灰显、逾期红点
- 进行中规划（判定：开始≤今天≤截止 且 进度<100%）在现在页顶部横幅轮播，组件风格随现在页，轮播间隔可配置（默认 6 秒）；横幅仅展示进度，不可编辑
- 规划附件上传规则沿用日记附件（20MB 上限）

### 5.6 视觉风格与自定义

| 页面 | 默认亮色 | 默认暗色 | 特征 |
|------|---------|---------|------|
| 现在 | 蓝白 | 蓝黑 | 方角扁平，无衬线，强调蓝；整洁高效 |
| 过去 | 深栗褐#221B12 | 墨棕#1A1510 | 深金史诗调，鎏金+暖象牙，胶片颗粒+金辉，衬线标题；庄严光辉（见 5.12） |
| 未来 | 深空黑#0A0A12 | 近纯黑#07070D | 赛博朋克，双色霓虹+平行四边形面板（75°竖边），扫描线+玻璃拟态，等宽字体（见 5.9） |

**用户自定义**：
- 每个页面可独立调色（色盘选取）
- 亮暗程度可调
- 未来页支持双色主题（适应赛博朋克风格）
- 组件可拖拽调整位置（两栏密铺基础不变）
- 顶栏位置可拖到左/右/下边缘

### 5.7 MD 编辑器统一规格

- 全系统复用同一套 MD 编辑器组件
- 图标后续替换为 SVG（可重新着色，适应不同页面风格）
- 核心行为：自动存草稿（每秒或内容变化）、实时保存、删除 ≥50 字时自动备份被删段落
- 以上行为对所有编辑器（公开/绝密）一致，可见性跟随内容本身权限

### 5.8 布局可调性（2026-09-03 确认）

- 卡片顺序可拖拽排序（身体/心情顺序无所谓，可互换）
- 每组卡片高度可调整
- 左右栏宽度可调整
- 所有分隔线均可拖拽改变间距
- 状态：UI 规则继续框定中，暂不开始开发
### 5.9 未来页赛博朋克风格规范（联网调研 2026-09-03）

**布局（游戏化，非工业仪表盘）**
- 平行四边形面板体系：水平边水平、竖边内倾约 75°，产生"前倾"感（实证参照：Cyberpunk 2077 HUD 倾斜面板）
- 拒绝对齐 dashboard 网格：元素围绕视觉重心倾斜排布、偏移轴线、部分重叠，面板间以斜切连接线（ribbon cable 风格）衔接
- 面板用四角 L 形角标框定（细 hairline），非连续矩形边框；完整矩形边框仅用于输入框/表格
- 零圆角、锐利切角；密度优先，信息填满像素

**色彩（双色主题基础）**
- 深近黑底：#0a0a0f / #0d0d12 级别，带蓝紫调（非纯黑）
- 霓虹强调色（每屏主色 1-2 种，克制使用）：青 #00fff5、品红 #ff00ff、电蓝 #0066ff、荧光黄、毒绿 #00ff41
- 语义色分角色：主操作色占主导 + 青色做状态/焦点 + 品红仅用于危险/警报
- 层级用发光表达（box-shadow/text-shadow 外发光），不用投影
- 背景图/轮播配置下，霓虹色需与背景保持对比度

**质感**
- 扫描线叠加层：低透明度、pointer-events: none（不得拦截点击）、不将对比度压到 WCAG 最低线以下
- CRT/数字噪点可选
- 字体：JetBrains Mono / Fira Code / IBM Plex Mono 等宽字体用于数据/标签/头部；展示标题可用全大写+宽字距压缩字体
- 玻璃拟态：半透明面板 + backdrop-blur + 霓虹描边
- 细网格背景（可带缓慢扫描动画）

**故障效果（glitch，仅用于过渡与关键高亮）**
- RGB 分离：主层白 + 红/青偏移层（mix-blend-mode: screen），data-text + ::before/::after 实现，避免 DOM 重复
- clip-path 水平切片随机位移 + translate 抖动；steps() 顿挫 vs cubic-bezier 振动按需选择
- 闪烁不超过 3 次/秒；必须提供 prefers-reduced-motion: reduce 降级（静态 RGB 偏移或纯淡入）
- 禁止整段正文故障化，只用于标题/单字/短标签

### 5.10 丝滑动画技术方案（2026-09-03 调研）

- 三屏切换主技术：View Transitions API（Chrome 111+/Safari 18.2+/Firefox 130+，基线支持）
  - document.startViewTransition() + CSS ::view-transition-old/new(root) 自定义动画
  - 浏览器原生 GPU 加速，低端设备性能优于 JS 动画库（实测参考：快 2-3 倍）
  - view-transition-name 固定元素身份，实现"日记卡片从列表 morph 到详情"等共享元素过渡
- 三屏切换动画设计：
  - 现在→未来：旧屏 glitch 切片消失（约 1.6s，强度 CSS 变量可调）+ 新屏霓虹光扫过淡入
  - 现在→过去：暖色旧纸光晕过渡（cubic-bezier 缓动）
  - 未来→现在/过去：反向
  - 视差：perspective 容器 + 背景整体 translateZ 慢于前景
- GSAP：保留给精确编排的组件动画（卡片拖拽排序、图表数据入场、日历弹窗）
- 无障碍：所有动画必须带 prefers-reduced-motion 降级分支，静态 RGB 偏移保留、位移动画禁用

### 5.11 页面背景自定义（2026-09-03 新增）

- 每个页面（三屏）独立配置背景：单图 / 多图轮播（可设间隔秒数）/ 纯色 / 渐变
- 背景图上传沿用日记附件上传安全规则（MIME 校验、大小限制、重命名）
- 配置存入 settings.json，用户全权配置，三屏互不影响

### 5.12 过去页"人生编年史"史诗风格规范（2026-09-03 确认）

**定调**：记录书写者一生的殿堂级编年史——展开一部厚重的个人史诗：宽银幕电影感、金色余晖、勋章与丰碑。与未来页（冷霓虹/锐利）形成对立，与现在页（蓝白扁平）区隔。

**色彩（深金史诗调）**
- 底色：深栗褐 #221B12 / 墨棕 #1A1510（非亮米白）
- 表面：暗金褐 #3A2F22（陈年皮革书封感）
- 文字：暖象牙 #F2E5C9；大标题鎏金 #D4AF37；高光香槟金 #FFE9A8
- 沧桑点缀：暗铜 #B87333，克制使用（低谷期标记）
- 图表色系：金→铜→褐渐变，禁用霓虹
- 暗色/亮色模式均基于此暖金系

**质感（沧桑=包浆，不是破损）**
- 背景：深褐底 + 电影暗角 + 细胶片颗粒 + 顶部缓慢流动的金色柔光（尘室斜阳感）
- 卡片：细金线描边 + 极浅内发光，展柜典藏感
- 里程碑：鎏金星芒/勋章节点带柔光晕（荣誉见证落点）
- 低谷期/挫折：暗铜色标记，庄重呈现

**磨损感（荣誉的见证，2026-09-03 补充确认）**
- 磨损只落在"荣誉载体"上：里程碑徽章带细微划痕纹理、边缘局部褪色、轻度哑光（被手掌反复摩挲过的奖牌）
- 章节年份标题如石碑铭文：笔画边缘微微风化，笔画深处保留金色
- 表面叠极淡"使用痕迹"噪点（比胶片颗粒更细、更局部），模拟多年翻阅触感
- 越重要的节点磨损越明显（被反复触摸=荣耀证明）
- 克制原则：磨损仅覆盖约 5% 面积的强调元素，大面积底子保持光洁庄严

**排版与布局**
- 过去页 = **可缩放人生时间轴**（竖向，默认左侧，可拖放到上/下/右，书写者专属布局）+ 主区域
- 时间轴范围：注册日 → 今天，不含更早与未来
- 方向：竖置今天在底部，横置今天在右端
- 交互：滚轮缩放（向上放大→天级；向下缩小→月/年级；最大时屏幕内保持 3~5 天）、左键拖动平移、点击精确选中某一天
- 里程碑在时间轴上金色勋章节点标注；"今天"有固定标记
- 主区域：顶部两个折线统计图组件（心情曲线+身体指数曲线，随时间轴范围联动，不标数值），下方选中日期的日记、里程碑、评论
- 标题：衬线重字重（Noto Serif SC），正文保持可读

**动画（缓慢、有重量）**
- 进入/切屏：金色光晕缓慢淡入，章节标题如电影字幕浮现
- 滚动：长卷推进，金色尘埃粒子缓慢漂浮
- 里程碑出现：金光扫过节点，克制不闪
- 全部带 prefers-reduced-motion 降级

---

### 5.13 登录页流程与动画（2026-09-03 确认）

**页面结构**（2026-09-03 确认：纯黑底 + 故障风 logo + 密钥输入框，无背景图）：
1. 故障风项目 logo（RGB 分离 glitch 闪烁，规格见视觉规格书 3.9）
2. logo 下方：密钥输入框（所有人均密钥登录，书写者输主密钥、阅读者输只读密钥；一长串随机字符串，粘贴登录）
3. 输入框下方：登录按钮（或回车提交）

**动画序列（密钥校验通过后）**：
1. logo 放大并淡出（scale 增大 + opacity→0）
2. 输入框变透明（0.5s）
3. 显示时段问候语："清晨好/早上好/中午好/下午好/晚上好/深夜好，<昵称>"（书写者显示其昵称，阅读者显示其密钥昵称）——先 opacity 降低出现，再 opacity 升高消失；淡入淡出统一 0.5s，问候语完成后停留 1s（时长均可设置）
4. 整页"现在"屏以页面正中为中心缩放淡入

**问候时段划分（2026-09-03 用户授权由实现者定义，不重要）**。

**会话**：记住登录直到手动登出，无过期。

**相关实体**：人生无名称（仅概念）；系统管理员见 /admin。

### 5.14 背景音乐歌单规格（2026-09-03 确认）

**行为**：
- 三页各有独立歌单（现在/过去/未来），进入页面自动播放该页歌单（首次登录点击后解锁浏览器自动播放）
- 切页换列表；切回原页**断点续播**（每页独立记忆当前曲目+播放进度）
- 标题栏一个"开始/停止播放"按钮，三页共用；按钮状态全局同步
- 播放不打断：切换页面时若停止状态则保持停止

**轮播模式**（设置中切换，默认列表循环）：列表顺序循环 / 随机播放 / 单曲循环

**音频管理**（设置页"歌单"tab）：
- 系统内置默认音频（三页默认音频不同，用户后续指定）+ 用户上传，均可删除
- 上传限制：MP3/OGG/FLAC/M4A/WAV，单曲 ≤20MB，每页歌单 ≤50 首

**音量**：百分比调节（默认 70%），三页共用

**权限**：无需权限，所有访问者可播放

### 5.15 通知中心（站内短信）（2026-09-03 确认）

- 触发源：新评论、密钥过期、规划到期、失联/停止状态变化、绝密解除（书写者自身操作提示）；**与待办无关**（待办到期走 APK 系统通知）
- 入口：顶栏铃铛图标 + 红点未读数
- 列表：时间倒序、未读高亮、点击跳转（评论→该日记；规划→详情；密钥→密钥管理）
- 保留策略：全部保留

### 5.16 移动端与 APK（2026-09-03 规划）

**总体**：手机端=浏览器直接访问 Web（响应式，功能与电脑一致）；不做套壳 WebView。iOS 不支持（仅安卓）。电脑端暂不做 exe（后续会有）。

**薄 APK（仅安卓）**：只做两件事——
1. **通知**：轮询服务器（WebSocket 排除，选轮询），检测待办到期等事件 → 系统通知
2. **语音转任务**：APK 内完成闭环——录音 → 系统 ASR（SpeechRecognizer）→ 文字可改 → LLM 结构化概括（用户自填 API key，key 存服务器）→ 预览 → 可回退手动编辑 → 存任务

**语音流程细节**：
1. APK 语音按钮 → 录音
2. ASR 识别转文字（系统引擎，仅安卓可用）
3. 用户可修改识别文字
4. 交给 LLM：口语 → 结构化任务 JSON（title/description/due_date/priority）
5. 预览确认，允许回退手动编辑
6. 保存为任务（走 Web API）

**待办提醒**：沿用现成日历/待办应用逻辑（每待办可设提醒时间、可重复），APK 系统通知。

**移动端布局**：手机不能用左右分栏；具体布局后议（暂不纠结）。

## 6. 技术栈选型

| 层 | 技术 | 理由 |
|----|------|------|
| 后端 | **Go + Gin** | 高效、单二进制部署、文件操作方便 |
| 前端 | **Vue 3 + Vite + TypeScript** | 动画生态好（GSAP/VueUse Motion），组件化 |
| 数据库 | **SQLite**（go-sqlite3 / modernc.org/sqlite） | 按月度分库，文件小便于移动 |
| 日历 | **现成开源日历/清单项目**（如 FullCalendar / vue-cal / Todo 类组件） | 2026-09-03 确认：直接抄现成实现，不纠结选型；现在页右栏用现成日历，未来页缩放日历自研 |
| MD 编辑器 | 开源 MD 编辑器（如 Milkdown/Editor.md） | 可定制，支持插件扩展 |
| 头像 | 前端 canvas 缩放 + 后端 nfnt/resize 压缩裁剪 | 安全限制：前端限大图，服务器二次校验 |
| 部署 | Go 单二进制 + 前端静态文件 | 简单可靠 |
| APK（安卓薄客户端） | Kotlin 原生（保活+通知+语音，无套壳 UI） | 2026-09-03 规划：轮询+SpeechRecognizer+LLM 概括 |

---

## 7. 安全设计

- 书写者密码：bcrypt/argon2 哈希
- 阅读密钥：随机生成 + bcrypt 哈希存储
- 传输安全：HTTPS
- 上传安全：限制 MIME 类型、分辨率、大小，重命名防路径穿越
- 会话：JWT（书写者）/ 密钥 token（阅读者）
- 权限检查：每个数据查询接口强制校验
- 绝密：仅服务端权限控制，磁盘明文（内网自用可接受）

---

## 8. 部署方案

- 内网 Linux 服务器（Docker 或直接运行）
- 远程通过内网穿透（frp/ZeroTier/Cloudflare Tunnel）
- 单二进制启动，自带 HTTP 服务
- 备份：按人生导出 → 月度 DB + 附件打包 → 输出到服务器配置路径

---

## 9. 开发阶段规划

### 第一阶段：基础框架
- Go 后端骨架 + Vue 3 前端骨架
- 用户注册/登录 + 密钥生成与登录
- global.db 建表
- 三屏基础路由与导航（顶栏指示器 + 翻屏箭头）
- 亮暗色切换框架

### 第二阶段：核心内容
- 日记 CRUD + MD 编辑器（自动存草稿、实时保存、删除备份）
- 心情记录（多 tag 体系 + 时间戳） + 曲线图
- 身体记录（打分 + 备注 + 曲线）
- 日历/日程 CRUD（FullCalendar 集成）
- 月度 SQLite 分库读写

### 第三阶段：权限与交互
- 三层权限校验（密钥级→条目级→绝密）
- 绝密模式（整体变红 + 平行内容层切换）
- 评论系统
- 里程碑标记
- 阶段目标

### 第四阶段：过去/未来页
- 过去页可缩放时间轴（注册日→今天、缩放/平移/点选）
- 心情/身体曲线（不标数值）
- 未来页日历 + 阶段目标
- 失联/停止状态机
- 主题自定义（色盘 + 深浅 + 未来双色）
- 组件拖拽 + 顶栏位置自定义

### 第五阶段：收尾
- 备份导出
- 头像上传 + 压缩裁剪
- 安全加固
- 内网部署文档
- 最终 UI 打磨 + SVG 图标替换

