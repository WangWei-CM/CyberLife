# Cyberlife 工程实施方案

> 文档版本：v1.0  
> 基线日期：2026-09-03  
> 适用范围：Web、Go 服务端、SQLite 数据层、Android 薄客户端  
> 当前状态：需求与视觉规格已形成，仓库刚初始化，尚未有业务源代码

## 1. 工程结论

Cyberlife 应先实现一个**模块化单体**，而不是一开始拆成微服务。原因是当前部署目标为内网单实例、SQLite、本地附件和单二进制；微服务会额外引入跨服务认证、事务、部署和备份复杂度，无法带来对应收益。

推荐基线：

- 后端：Go 1.24+、Gin、`database/sql`、SQLite 驱动、Argon2id、结构化日志。
- 前端：Vue 3、Vite、TypeScript、Pinia、Vue Router、统一设计令牌；图表与编辑器优先采用成熟库。
- 数据：`global.db` 保存跨人生身份与索引；每个人生按月一个 SQLite 数据库，设置和大文件放文件系统。
- 客户端：Web 为完整产品；Android 为原生 Kotlin 薄客户端，仅负责轮询提醒和语音转任务，不嵌入 WebView。
- 部署：Go 服务端提供 API 和静态文件，反向代理负责 HTTPS；运行数据、上传、备份必须位于应用二进制目录之外。
- 交付顺序：先做权限正确的垂直切片，再做视觉动效和可调布局。所有页面必须通过同一权限服务取数，前端不能自行判断可见性。

## 2. 需求基线与冲突处理

以 `策划书.md` 和 `questions.md` 中日期较新的最终确认项为准，`视觉规格书.md` 作为视觉实现基线。以下决策在编码前冻结：

| 事项 | 采用方案 | 原因与影响 |
|---|---|---|
| 书写者登录 | 主密钥登录，不使用用户名密码；密钥由 Admin 创建和补发 | `questions.md` 后续批次明确“所有人均密钥登录” |
| 阅读者登录 | 阅读密钥直登，只读，可评论但不可写入业务内容 | 与角色模型一致 |
| Admin 登录 | `/admin` 独立密码，密码仅存哈希 | 与普通人生会话隔离 |
| 人生名称 | 不设业务名称，界面使用固定的 Cyberlife/昵称等表达 | 需求明确人生只是概念 |
| 心情/身体补记 | 不允许回填；按服务器 `Asia/Shanghai` 当前时间写入 | 防止历史曲线被任意补写；日记可按日期补记 |
| 阶段目标 | 与长期规划合并为 `Plan` | 后续批次已确认 |
| 月库 | 记录按“归属日期”路由；创建时间不改变历史归属月 | 保证补记和权限锚点语义稳定 |
| 权限锚点 | 阅读密钥创建时刻为最低门槛；内容按服务器本地日期判断 | 需求同时规定时间锚点和按日期判定，采用“日期门槛”并保留原始时间 |
| 无权限内容 | API 结果中完全过滤，不返回 ID、标题、计数等存在性信息 | 需求接受曲线断点和日历空白，但不接受直接内容泄露 |
| 站内消息 | 只处理评论、密钥过期、规划到期、人生状态变化、绝密解除；待办到期只给 Android 系统通知 | 以第七批和移动端最终确认项为准 |
| 提醒调度 | 服务端保存提醒定义；Android 通过轮询领取事件并本地发通知 | 不引入 WebSocket 和常驻服务 |
| LLM 密钥 | 第一版服务端加密存储，接口永不返回明文；后续可改为设备侧存储 | 需求明确 key 存云，但这是高风险项，需增加密钥加密和轮换 |
| 动效 | View Transitions 作为增强，所有导航有无 API 的普通切换回退 | 不能让浏览器兼容性阻断主流程 |

### 2.1 必须在第 0 阶段确认的剩余决策

这些问题不阻塞骨架，但必须在相应阶段开始前写入决策记录：

1. Admin 初始密码如何注入：环境变量、首次启动引导，还是安装脚本生成。
2. ReaderKey 过期边界采用包含还是不包含；建议 `now >= expires_at` 立即失效。
3. 重复任务采用 RRULE 子集还是固定展开；建议保存规则并按窗口展开，不预生成 100 年数据。
4. 音频默认文件的版权与初始内容；技术实现不预置未经授权的音频。
5. LLM 提供商、模型、超时、费用上限和失败后的手动回退策略。
6. 数据导入是覆盖式还是合并式；第一版建议仅支持同一 `life_id` 的完整校验包覆盖恢复。
7. 移动端是否需要设备撤销、单设备密钥和轮询频率；建议每设备独立 token，可单独撤销。

## 3. 总体架构

```text
                           +---------------------+
                           |  Browser / Vue SPA  |
                           +----------+----------+
                                      | HTTPS / JSON
                           +----------v----------+
                           | Go Modular Monolith |
                           | Gin + Auth + ACL    |
                           +--+-------+-------+---+
                              |       |       |
                   +----------+       |       +----------+
                   |                  |                  |
           +-------v-------+  +-------v-------+  +-------v--------+
           | global.db     |  | life DB pool  |  | File storage   |
           | admin/writer  |  | YYYY-MM.db    |  | uploads/backup |
           | reader/life   |  | records       |  | safe paths     |
           +---------------+  +---------------+  +----------------+
                                      ^
                                      |
                           +----------+----------+
                           | Android thin client|
                           | polling + ASR + LLM |
                           +---------------------+
```

### 3.1 服务端分层

建议目录：

```text
server/
  cmd/cyberlife/main.go              # 进程入口、配置、依赖装配、优雅退出
  internal/
    config/                          # 环境变量、默认值、启动校验
    http/
      router.go                      # 路由、middleware、静态文件
      middleware/                    # request-id、认证、限流、错误恢复
      handler/                       # 只做 HTTP 入参/出参转换
      response/                      # 统一错误和分页格式
    auth/                            # key 解析、哈希校验、session、Admin auth
    acl/                             # 锚点、预设、绝密、评论权限统一判定
    life/                            # Life 状态和活跃度
    diary/                           # 日记、草稿、删除备份、附件关联
    mood/                            # 心情 tag、记录和聚合
    body/                            # 身体记录和聚合
    task/                            # 任务、重复规则、提醒、里程碑
    plan/                            # 长期规划、进度历史、附件
    comment/                         # 评论和回复
    notification/                    # 站内通知和事件消费
    playlist/                        # 歌单、曲目、播放设置
    setting/                         # 页面主题、布局、背景、用户设置
    backup/                          # 导出、导入、校验、审计
    mobile/                          # Android 轮询 API、设备 token
    storage/                         # DB manager、月库路由、文件存储
    event/                           # 进程内领域事件/事务后任务
  migrations/
    global/
    life/
  web/                               # 构建后的前端静态文件或嵌入入口
web/
  src/
    app/                             # 根布局、路由、主题和错误边界
    api/                             # HTTP client、类型、错误处理
    stores/                          # auth、settings、player、notification
    components/                      # 通用编辑器、附件、评论、权限控件
    features/
      now/
      past/
      future/
      settings/
      admin/
    styles/                           # tokens、page themes、motion、a11y
android/
  app/                               # Kotlin 原生薄客户端
```

后端 handler 不直接写 SQL；handler 调用 application/service，service 调用 repository。ACL 不散落在 repository 或前端，而由 service 在查询和写入前统一执行。跨月查询由 `storage` 提供聚合接口，各领域不自行拼接数据库文件路径。

### 3.2 请求处理链

```text
HTTP
 -> request-id / recovery / access log
 -> session authentication
 -> life scope resolution
 -> writer/reader/admin capability
 -> service validation
 -> ACL query or command
 -> repository transaction
 -> domain event / notification enqueue
 -> response DTO
```

所有写操作都要带服务端生成的 `request_id` 或幂等键；日志记录请求 ID、操作者类型、life ID、目标类型和结果，不记录原始密钥、正文、附件内容或 LLM key。

## 4. 身份、会话与权限架构

### 4.1 密钥与会话

- 数据库只保存 `key_hash`，推荐 Argon2id；若采用 bcrypt，参数必须集中配置并有升级策略。
- 主密钥和阅读密钥都由服务端使用 CSPRNG 生成，展示一次或由 Admin 受控复制；数据库不保存明文。
- 登录接口采用恒定时间比较语义。错误统一为“密钥无效或已失效”，不能暴露是不存在、过期还是作废。
- 登录成功后签发随机不可预测的 session token。建议 token 只在 HttpOnly、Secure、SameSite=Lax Cookie 中传输；Android 使用独立 bearer token。
- 服务端 session 表保存 token 哈希、身份类型、life ID、创建时间、最后活动时间、撤销时间、设备信息摘要。
- “记住登录直到手动登出”只表示不设置普通短期过期；作废密钥、登出、管理员撤销设备后必须立即失效。可设置非常长的绝对上限作为运维保护，并在文档中公开。
- ReaderKey 的过期、作废和 Writer 补发都通过 session 版本或撤销时间即时生效。

推荐登录接口：

```text
POST /api/v1/auth/key-login       { key }
POST /api/v1/admin/auth/login      { password }
POST /api/v1/auth/logout
GET  /api/v1/auth/me
```

### 4.2 ACL 判定顺序

统一函数：

```text
CanRead(actor, resource) -> (bool, reason-internal)

1. actor/session 有效？否 -> 拒绝
2. 资源是否属于 actor.life？否 -> 拒绝
3. resource.secret == true 且 actor != writer？否 -> 拒绝
4. 对 mood/body：归属本地日期 >= key.anchor_local_date？否则拒绝
5. 对 diary/task/plan/attachment：
   5.1 归属本地日期 >= key.anchor_local_date？否则拒绝
   5.2 若存在权限预设，actor key 是否被允许？否则拒绝
6. 公开且在锚点范围内 -> 允许
```

注意：

- 锚点是最低门槛，预设不能放大可见范围。
- 心情和身体不提供条目级预设，但 `secret` 仍逐条生效。
- 附件是独立资源。创建时复制父条目的权限快照，之后拥有自己的权限字段；读取下载接口必须重新走附件 ACL，不能因为拿到父条目就直接放行。
- 里程碑的“标志是否存在”和关联日记/任务是两个可见性判断；标志不可见时，时间轴不能显示节点。
- 评论属于目标条目的子资源。只有能读目标且目标 `commentable=true` 的 actor 才能创建；删除权限为评论作者本人或 Writer。
- 列表、统计、搜索、分页总数、时间轴节点、曲线数据都必须过滤后再聚合，避免通过数量和位置推断绝密或无权内容。

### 4.3 权限预设更新策略

权限预设使用关系表而不是 JSON 主键集合，避免删除 key 后出现悬空 ID：

```text
permission_presets(id, life_id, name, created_at, updated_at)
preset_key_rules(preset_id, reader_key_id, allowed, updated_at)
```

删除 ReaderKey 时保留审计信息并将规则标记为失效，不能复用原 ID。修改预设只影响后续读取，不复制历史内容。权限变更应记录审计事件；不向 Reader 暴露预设名中可能含有的敏感信息。

## 5. 数据架构

### 5.1 global.db

保存不依赖月份且用于登录/路由的数据：

```text
admins
writers
reader_keys
lives
sessions
mobile_devices
life_months                  # 已创建月库、schema 版本、校验状态
backup_jobs
audit_events
```

建议关键字段：

- `writers`: `id`, `nickname`, `master_key_hash`, `key_version`, `created_at`, `revoked_at`。
- `reader_keys`: `id`, `life_id`, `key_hash`, `nickname`, `avatar_file_id`, `anchor_at_utc`, `anchor_local_date`, `expires_at`, `revoked_at`, `note`, `created_at`。
- `lives`: `id`, `owner_id`, `status`, `last_active_at`, `created_at`, `stopped_at`。
- 所有时间保存 UTC；业务日期另存 `date` 或通过固定 `Asia/Shanghai` 转换，禁止依赖运行机器时区。

### 5.2 月度 life DB

每个 `{life_id}/YYYY-MM.db` 使用同一迁移版本，建议表：

```text
schema_migrations
 diary_entries
 diary_revisions                     # 草稿、自动保存和删除段落备份
 content_attachments
 mood_records
 body_records
 tasks
 task_recurrence_rules
 task_reminders
 milestones
 plans
 plan_progress
 comments
 inbox_messages
 notification_deliveries             # 可选，用于事件去重
 mood_tags
 body_tags
 playlists
 playlist_tracks
```

通用字段建议：

```text
id TEXT/INTEGER PRIMARY KEY
life_id TEXT NOT NULL                 # 月库内仍保留，防错库
created_at TEXT NOT NULL              # UTC RFC3339
updated_at TEXT NOT NULL
secret INTEGER NOT NULL DEFAULT 0
```

领域字段补充：

- `diary_entries`: `entry_date`, `content_md`, `visibility_preset_id NULL`, `commentable`, `milestone_id NULL`。同一天允许一条公开日记和一条绝密日记，应用层强制唯一约束。
- `mood_records`: `recorded_at_utc`, `recorded_local_date`, `value`, `note`, `tag_snapshot_json`。删除 tag 后历史值仍可展示，名称和 emoji 不从历史快照恢复为当前 tag。
- `body_records`: `recorded_at_utc`, `recorded_local_date`, `score`, `note`；禁止客户端指定历史日期，服务端按当前时间写入。
- `tasks`: `task_date`, `due_at_utc NULL`, `title`, `description`, `status`, `priority`, `visibility_preset_id`, `commentable`, `recurrence_rule_id NULL`, `completed_at_utc NULL`。
- `plans`: `start_date`, `end_date`, `name`, `intro_md`, `visibility_preset_id`, `secret`, `commentable`。跨月规划按 `plans` 所属 life 的稳定索引查询，建议在 global.db 的 `life_months` 旁增加规划索引，或建立专用 `life_catalog.db`；不能只依赖某一个月库。
- `plan_progress`: `(plan_id, progress_date)` 唯一，`percent` 0-100；同日覆盖使用 UPDATE，保留变更审计。
- `comments`: `target_type`, `target_id`, `parent_id NULL`, `author_key_id`, `content`, `deleted_at`。回复深度第一版限制为一层。
- `inbox_messages`: `recipient_type`, `recipient_id`, `type`, `ref_id`, `text`, `read_at NULL`, `created_at`。站内消息全部保留，但必须有游标分页。

### 5.3 月库路由与跨月查询

定义统一 `MonthKey`：

```text
MonthKey = YYYY-MM in Asia/Shanghai
```

- 写入日记/任务/规划进度等带业务日期的记录时，服务端计算 MonthKey；客户端传入的日期只作为值校验，不允许指定数据库文件名。
- 读取范围先根据起止本地日期生成有限月列表，再由 DB manager 依次查询、ACL 过滤、按 `(event_at, id)` 合并排序。
- API 默认限制最大查询跨度，例如 24 个月；过去页时间轴需要更大范围时采用按月窗口加载，不一次打开全部月库。
- DB manager 维护有上限的连接池/句柄缓存，并在空闲时关闭；每次应用启动扫描并校验 `life_months`，不自动执行破坏性迁移。
- SQLite 开启 WAL、busy timeout 和外键约束。写事务短小，附件写入成功后再提交记录，失败时清理未引用文件。
- 不使用跨文件 SQLite transaction。涉及 global.db 与月库的操作使用 outbox/状态机：先写主记录，再写可重试的事件；恢复流程必须幂等。

### 5.4 设置与文件系统

```text
data/
  global.db
  lives/{life_id}/YYYY-MM.db
  lives/{life_id}/settings.json
uploads/{life_id}/
  avatars/
  diary/YYYY-MM/
  plans/YYYY-MM/
  audio/
backups/{life_id}/
```

- `settings.json` 只放非关键 UI 配置：页面主题、背景、布局、歌单排序和播放器断点；服务端读取后做 schema 校验和大小限制。
- 文件名采用服务端 UUID，原始文件名只作为显示元数据，禁止拼接用户输入路径。
- 上传流程：限制请求体大小 → 临时文件 → 魔数/MIME 检测 → 图片尺寸和音频格式校验 → 病毒扫描接口预留 → 原子移动 → 写附件记录。
- 下载使用授权后的文件流或短时签名 URL；短时 URL 必须绑定 life、文件和 actor，不能直接暴露可猜路径。

## 6. API 契约

### 6.1 统一响应和错误

成功响应建议直接返回资源 DTO；列表统一：

```json
{
  "items": [],
  "next_cursor": "opaque-token"
}
```

错误统一：

```json
{
  "error": {
    "code": "validation_failed",
    "message": "请求参数无效",
    "request_id": "...",
    "fields": {"title": "不能为空"}
  }
}
```

首批错误码：`unauthorized`, `forbidden`, `not_found`, `validation_failed`, `conflict`, `rate_limited`, `file_rejected`, `dependency_unavailable`, `internal_error`。对 Reader 隐藏资源存在性时，统一返回 `not_found`。

### 6.2 API 分组

```text
/api/v1/auth/*
/api/v1/me/*
/api/v1/life/summary
/api/v1/life/status
/api/v1/diaries
/api/v1/diaries/{id}
/api/v1/diaries/{id}/draft
/api/v1/diaries/{id}/attachments
/api/v1/moods/tags
/api/v1/moods/records
/api/v1/moods/series?from=&to=
/api/v1/body/tags
/api/v1/body/records
/api/v1/body/series?from=&to=
/api/v1/tasks
/api/v1/tasks/{id}
/api/v1/tasks/{id}/complete
/api/v1/tasks/{id}/reminders
/api/v1/plans
/api/v1/plans/{id}
/api/v1/plans/{id}/progress
/api/v1/milestones
/api/v1/comments
/api/v1/comments/{id}
/api/v1/notifications
/api/v1/notifications/{id}/read
/api/v1/settings
/api/v1/playlists/{page}
/api/v1/files
/api/v1/backups
/api/v1/mobile/poll
/api/v1/mobile/task-drafts
/api/v1/admin/writers
/api/v1/admin/reader-keys
/api/v1/admin/backups
```

契约要求：

- 所有资源 DTO 都不返回密钥明文、内部文件路径、权限规则中不必要的 key hash。
- 列表采用 cursor，不使用跨月 `OFFSET` 深分页。
- `PUT` 用于完整替换，`PATCH` 用于局部修改；写接口支持 `Idempotency-Key`。
- 删除优先软删除，保留审计和备份可恢复性；任何真正删除操作若未来开放，必须单独的确认和权限流程。
- 并发更新使用 `version` 或 `updated_at` 条件，冲突返回 409，不静默覆盖他人/其他设备修改。

## 7. 前端架构

### 7.1 路由与状态

```text
/login
/now
/past
/future
/settings/:tab
/admin
```

Pinia store 建议拆分：

- `auth`: actor、capabilities、life status、secret mode。
- `settings`: 三页主题、亮暗三档、背景、布局、分隔线位置。
- `player`: page playlist、曲目、音量、每页断点、播放状态。
- `notifications`: 未读数、游标列表、已读操作。
- 各 feature store：只持有页面查询状态，不重复保存权限判定逻辑。

API client 统一处理：Cookie/bearer、request ID、409、401 跳转、游标分页、AbortController 取消旧查询。服务端返回的 `capabilities` 只用于改善 UI；服务端始终是最终权限裁决者。

### 7.2 组件边界

```text
AppShell
  LoginFlow
  TopNav
    PageSwitcher
    MusicControl
    NotificationBell
    ActorMenu
    SecretModeToggle
  PageViewport
    NowScreen
      ClockPanel / PlanBanner / BodyCard / MoodCard / DiaryEditor / CalendarAdapter
    PastScreen
      TimelineAxis / TrendCharts / DayDiary / DayTasks / Comments
    FutureScreen
      PlanList / PlanDetail / ProgressHistory / ZoomCalendar
  SettingsScreen
  AdminScreen
```

统一组件优先建设：`MarkdownEditor`、`AttachmentPicker`、`PermissionEditor`、`CommentThread`、`ProgressBar`、`DateRangePicker`、`Modal`、`Toast`、`EmptyState`、`ConfirmDialog`。

### 7.3 编辑器数据流

Markdown 编辑器必须区分三种状态：

1. `draft`: 本地输入状态，防抖保存到浏览器本地和服务端草稿接口。
2. `saved`: 服务端正式内容，实时保存成功后更新版本号。
3. `deletion_backup`: 删除超过 50 个字符时由服务端根据前后版本生成备份片段；前端不自行判断最终删除量。

公开与绝密内容使用不同资源 ID/层，不在同一个编辑器状态里用 CSS 隐藏一层正文。Reader 永远不会下载绝密草稿。

### 7.4 三屏实现顺序

- Now 先完成真实数据的时间、身体、心情、日记、FullCalendar 适配；顶部计划横幅只消费已完成的 Plan 查询。
- Past 先做日期范围选择和虚拟化时间轴，再做滚轮缩放、拖动和曲线联动。时间轴使用日期到像素的纯函数，便于单测。
- Future 先完成计划详情/进度/任务 CRUD，再实现 7 天到 100 年的缩放模型。宽范围不渲染每一天的 DOM，使用月/季/年聚合。
- 拖拽布局只允许 Writer 保存；Reader 读取 Writer 已保存布局但不能写回。布局数据必须有版本和默认值，坏配置自动回退。

### 7.5 视觉与无障碍门槛

- 颜色只通过设计令牌和页面主题类注入，禁止组件内散落硬编码。
- 三屏风格变体复用语义组件，不复制三套业务逻辑。
- 所有图表提供无障碍摘要或表格替代；曲线可不显示数值，但屏幕阅读器必须获得趋势描述。
- 扫描线、噪点、glitch 层 `pointer-events: none`，不遮挡点击；提供 `prefers-reduced-motion` 分支。
- 移动端切为单列，固定格式控件使用稳定尺寸，不能因文字、图标或加载态造成布局跳动。

## 8. Android 薄客户端架构

模块：

```text
android/app/
  auth/              # 设备注册、token 保存、撤销
  polling/           # WorkManager 周期轮询、退避、网络约束
  notifications/     # Android notification channel、去重、点击深链
  speech/            # SpeechRecognizer 录音和权限
  taskdraft/         # 文本编辑、LLM 请求、JSON 校验、预览确认
  api/               # 与 Web 相同的任务/轮询契约
```

流程：

1. 用户绑定设备，服务端签发设备 token。
2. WorkManager 在网络可用时轮询 `/api/v1/mobile/poll`，提交 `cursor` 和设备能力。
3. 服务端返回待办提醒事件及下一次建议轮询时间；客户端按 `event_id` 去重并创建系统通知。
4. 通知点击打开 Web 深链；若无网络，保留待处理状态。
5. 语音流程为录音 → Android `SpeechRecognizer` → 用户修改文本 → LLM 结构化 → 严格 JSON Schema 校验 → 预览 → 调任务 API 保存。
6. LLM 失败、超时、输出非法或用户拒绝时，保留原始识别文本并回退手动任务编辑。

服务端不能把“已通知”仅存内存；事件领取和确认接口必须幂等。Android 端不保存主密钥，设备 token 可从 Admin/Writer 侧撤销。

## 9. 任务阶段与交付物

### 阶段 0：工程基线与决策冻结

**目标**：建立可重复运行的仓库和契约，消除高风险歧义。

工作项：

- 建立 Go module、Vue workspace、Android module、统一 README 和 `.env.example`。
- 确定配置层、日志、错误码、时区、ID、迁移命令、数据目录和端口。
- 写 OpenAPI 或等价 TypeScript/Go 契约；建立 API 生成/校验流程。
- 确认上述 7 项剩余决策并记录 ADR。
- 配置格式化、lint、单测、构建和 CI 基础流水线。

交付物：可启动空服务、登录页占位、数据库迁移命令、ADR、CI。

质量门禁：干净环境可一条命令启动；迁移可重复执行；无明文 secret 进入 Git；前后端契约检查通过。

### 阶段 1：身份、人生空间与数据基础

**目标**：完成 Admin 创建 Writer、密钥登录、会话和月库生命周期。

工作项：

- global.db 迁移、Admin 首次初始化、Writer/ReaderKey CRUD。
- Argon2id 密钥哈希、session cookie、登出和撤销即时生效。
- life 月库创建、迁移、连接管理、WAL、备份锁。
- `GET /me` 返回角色、能力、life 状态和时区。
- 基础 Admin 页面和 Writer 登录/登出。

质量门禁：登录暴力尝试限流；作废 key 的已有会话不能继续读取；Reader 访问 Admin、跨人生资源均失败；重启后会话与月库状态符合设计。

### 阶段 2：Now 垂直切片

**目标**：完成一条真实可用的记录闭环。

工作项：

- Clock（Asia/Shanghai）、MoodTag/BodyTag 管理。
- 心情分钟记录和今日趋势；身体 0-100 记录和近 7 日趋势。
- 日记 Markdown、草稿、实时保存、删除段落备份。
- 任务 CRUD、完成状态、现在页日历适配。
- 文件上传、附件独立资源与下载授权。

质量门禁：Writer 可写，Reader 所有写接口拒绝；心情/身体不可补记；记录落入正确月库；附件无法越权下载；刷新和断网重连不会丢失已保存内容。

### 阶段 3：ACL、绝密、评论与里程碑

**目标**：在扩大 UI 前锁定安全边界。

工作项：

- 三层 ACL 服务和表驱动测试。
- 权限预设、创建 key 时权限初始化、预设更新。
- 公开/绝密平行日记和绝密心情/身体。
- 条目评论、回复、删除权限、评论通知。
- 里程碑可见性与描述/详细信息分离。

质量门禁：至少覆盖 Writer、两个 Reader、不同锚点、预设允许/拒绝、绝密、附件继承后独立修改等矩阵；无权资源不会出现在列表、曲线、时间轴或总数中；审计日志不含正文和密钥。

### 阶段 4：Past 时间轴与历史聚合

**目标**：将月库查询、ACL 过滤和时间轴交互结合起来。

工作项：

- 范围加载 API、跨月游标和趋势聚合。
- 时间轴日期↔像素模型、天/月/年刻度、滚轮缩放、拖动平移、点选日期。
- 默认今天、注册日到今天边界、里程碑节点。
- 选中日左右对半显示日记与任务、评论。
- Writer 布局保存、Reader 只读应用布局。

质量门禁：注册日前和今天后的日期不可选；缩放极值满足 3-5 天和全范围要求；大量月份不阻塞主线程；无权数据不改变可观察节点和曲线数值；移动端降级为可操作的历史列表/单列。

### 阶段 5：Future 规划、进度与缩放日历

**目标**：完成规划和长期任务闭环。

工作项：

- Plan CRUD、简介 Markdown、附件、权限和绝密。
- 每日进度覆盖、历史时间线、时间进度和计划进度计算。
- 现在页进行中规划横幅只读跳转。
- 任务提醒时间、重复规则和逾期状态。
- 未来页 7 天至 100 年的分辨率模型、聚合渲染和点击创建。

质量门禁：时间进度在开始前/结束后有明确钳制规则；同日进度幂等覆盖；100 年范围不生成海量日单元；重复任务在窗口内不重复；规划和附件 ACL 一致。

### 阶段 6：设置、播放器、通知和状态机

**目标**：补齐跨页面基础能力。

工作项：

- 三页独立歌单、默认音频、上传限制、循环模式、音量和断点续播。
- 三档主题模式：黑、白、日出日落；每页颜色与背景配置。
- 组件、分隔线、顶栏位置布局保存。
- 站内通知中心、未读数、跳转、全部保留。
- 7 天失联、30 天停止、自动恢复和手动撤销。
- 设置页按角色隐藏敏感 tab，但服务端同样禁止接口访问。

质量门禁：切页播放状态符合规则；无效设置自动回退；通知事件有唯一键不重复；状态边界按 Asia/Shanghai 测试；Reader 无法通过直接请求访问隐藏设置。

### 阶段 7：Android MVP

**目标**：实现通知和语音建任务的最小闭环。

工作项：

- 设备绑定、token 撤销、轮询退避和系统通知渠道。
- 提醒事件生成、确认、去重、深链。
- ASR 文字编辑、LLM schema、预览确认和任务保存。
- 无网络、权限拒绝、ASR/LLM 失败的回退。

质量门禁：重复轮询不重复通知；服务端事件重放安全；设备撤销立即生效；LLM 返回任意非预期字段不会直接写入任务；Android 低电量/无网络可恢复。

### 阶段 8：备份、加固、部署与发布

**目标**：形成可维护的内网发布包。

工作项：

- 按人生导出 global 索引、月库、settings、附件和 manifest/checksum。
- 导入前校验版本、life ID、路径和哈希；失败不覆盖现有数据。
- HTTPS、反向代理、CSP、上传安全、限流、审计、日志脱敏。
- Docker 和直接运行两种部署文档，systemd/Windows 服务二选一作为首发目标。
- 恢复演练、升级回滚、健康检查和容量监控。

质量门禁：在隔离目录完整恢复后关键查询结果一致；备份包被篡改可检测；恶意文件名/MIME/超限请求被拒绝；生产配置无默认密码；发布包可从零部署。

## 10. 测试策略

### 10.1 单元测试

- ACL：使用表格覆盖所有角色、锚点、预设、绝密、附件和评论组合。
- 月库：日期到 MonthKey、跨年、时区边界、连接缓存和迁移版本。
- 进度：开始前、进行中、截止日、截止后、无记录、覆盖记录。
- 时间轴：日期与像素双向映射、缩放上限/下限、点击日期误差。
- 重复任务：窗口展开、时区、逾期和幂等事件。
- 文件：魔数、尺寸、扩展名欺骗、路径规范化。

### 10.2 集成测试

使用临时数据目录启动真实 HTTP 服务和 SQLite：

- 登录 → 创建/作废 key → 访问内容。
- 写入跨月日记/任务 → Past 范围查询 → ACL 过滤。
- 上传附件 → 修改独立权限 → Reader 下载验证。
- 评论创建 → Inbox 消息生成且不重复。
- 备份导出 → 隔离目录导入 → 校验数据和文件。

### 10.3 前端与端到端测试

- Playwright 覆盖登录、三屏切换、绝密模式、日记保存、计划编辑、通知跳转、Reader 禁止写入。
- 视觉回归覆盖 Now/Past/Future 的亮暗模式、secret mode、桌面和单列移动布局。
- 时间轴拖动、滚轮缩放、未来日历点击需在真实浏览器测试。
- `prefers-reduced-motion`、键盘导航、焦点可见、错误和空状态必须有测试。

### 10.4 安全测试

- 登录限流、会话固定、CSRF、CORS、Cookie 属性、IDOR、越权附件、跨人生读取。
- SQLite 路径注入、任意文件写入、压缩包路径穿越、伪造 MIME。
- 通知和搜索存在性泄露；绝密内容在响应、缓存、日志和错误中泄露。
- 依赖漏洞扫描和发布前手工权限矩阵复测。

## 11. 部署与运维

### 11.1 运行约束

- 反向代理终止 HTTPS，Go 服务只监听内网回环或受限网卡。
- `data/`、`uploads/`、`backups/` 使用独立持久卷；应用用户只授予必要读写权限。
- 配置通过环境变量或受权限保护的配置文件注入；不把 Admin 密码、加密密钥、LLM key 写入仓库。
- 健康接口分为存活检查和就绪检查；就绪检查包含 global.db、迁移版本和数据目录权限。
- 日志使用 JSON，按 request ID 关联；对正文、原始密钥、Authorization、Cookie、文件内容做脱敏。

### 11.2 备份与恢复

备份不是简单复制正在写入的 SQLite 文件：

1. 进入 life 级读写协调器，阻止该人生短写事务。
2. 使用 SQLite backup API 或一致性快照复制 global 相关记录、月库和文件。
3. 生成 manifest、schema 版本和 SHA-256。
4. 原子写入固定备份目录，完成后解除协调。
5. 恢复到新目录并执行完整性检查、外键检查和附件引用检查。
6. 恢复演练成功后才允许覆盖式导入。

导出接口只创建后台任务并返回 job ID，不让 HTTP 请求长时间阻塞；状态和错误可查询，所有路径由服务端生成。

## 12. 主要风险与应对

| 风险 | 等级 | 应对 |
|---|---:|---|
| 月库跨库查询与规划/通知索引复杂 | 高 | 先实现 MonthRouter、范围查询和测试；跨月实体增加 life 级索引 |
| ACL 误差导致隐私泄露 | 极高 | 单一 ACL 服务、默认拒绝、过滤后聚合、矩阵测试和安全复测 |
| “永久记住登录”与密钥作废冲突 | 高 | session 可即时撤销；按 key 版本/撤销时间校验 |
| 服务端保存 LLM key | 高 | 应用层加密、密钥不回显、轮换/撤销、访问审计；后续评估设备侧方案 |
| 100 年日历渲染性能 | 高 | 分辨率模型、月/年聚合、窗口化和虚拟滚动；不生成全量天 DOM |
| UI 可调布局导致状态不可控 | 中 | 布局 schema 版本化、范围校验、默认回退、只允许 Writer 保存 |
| 浏览器自动播放限制 | 中 | 首次用户手势解锁 AudioContext；停止状态全局保存，不能强制播放 |
| 上传文件成为攻击入口 | 高 | 魔数校验、大小/尺寸限制、UUID 路径、隔离存储、下载鉴权 |
| SQLite 文件损坏或备份不一致 | 高 | WAL、短事务、一致性快照、定期恢复演练和 checksum |
| 需求继续变化造成返工 | 中 | ADR + 阶段门禁；先冻结领域模型和 API，再做视觉细节 |

## 13. 首个可验收版本（MVP）

MVP 不包含 Android、复杂动效、可调顶栏、100 年完整缩放和自定义背景轮播。MVP 必须包含：

- Admin 创建 Writer 和 ReaderKey。
- Writer/Reader 登录、登出、作废 key 即时失效。
- Now：日记、心情、身体、任务和基础日历。
- 月库写入/读取和跨月基础范围查询。
- ACL：锚点、预设、绝密、附件独立授权。
- Past：日期范围、今天默认选中、日记/任务查看和两条趋势曲线。
- Future：Plan、进度、基础任务日历。
- 基础评论、站内通知、备份导出。
- 桌面与单列移动布局、键盘可用、减少动效模式。

MVP 验收不以“页面看起来完成”为标准，而以权限矩阵、真实 SQLite、刷新恢复、附件下载和备份恢复测试通过为准。

## 14. 推荐开工顺序

1. 保留当前三份需求/视觉文档，使用本方案作为工程实现基线。
2. 完成阶段 0 的 ADR、契约、配置、目录和 CI。
3. 直接做阶段 1 的身份 + 月库，再做阶段 2 的 Now 垂直切片。
4. 在任何公开测试数据前完成阶段 3 ACL；禁止先做一个“默认全公开”的临时权限实现并长期保留。
5. Past/Future 在真实 API 和权限过滤之上开发，图表和时间轴不接假数据作为最终实现。
6. 每阶段通过质量门禁后再进入下一阶段；阶段之间保留可部署标签和数据库迁移版本。

本方案的核心验收原则是：**所有用户可见内容先经过服务端授权，再经过聚合和渲染；所有可恢复数据先有一致性策略，再提供编辑和导出；所有视觉复杂度建立在稳定领域接口之上。**
