# Cyberlife 前端整改军令状 · 三期（v1.0）

> 命令对象：前端实现 AI。
> 依据：`策划书.md`、`视觉规格书.md`、一期+二期军令状、本文件。本文件是**三期增量**，一、二期已达标项禁止回退。
> 本文件主题：**动效与视觉质感**。正式判定：你对"动效"和"好看"的理解远未达标，本期只做一件事——让每个交互都有过渡、每个页面都有层次。

---

## 0. 现状判决（先承认这些）

经核查 `web/src/style.css`，当前全部动画资产只有 8 个 keyframes：
`glitch`、`leave`、`greet`、`spin` + 4 个 view-transition（vt-glitch-out / vt-neon-in / vt-past-out / vt-past-in）。

**这意味着**：页面切换之外的几乎所有交互都是**瞬间跳变**，包括：
- 过去页时间轴缩放：滚轮 = `Math.round(spanDays × 0.7 / 1.4)` 取整跳变，35 天处**硬切** day→month，730 天处**硬切** month→year。滚一下从 30 天直接变 21 天再变 14 天，视觉上是"啪"地重排。
- 登录输入框：普通原生 input，无渐变框、无聚焦动画、无质感。
- 列表/卡片/弹层/进度条/曲线/待办勾选：全部无过渡。

本期目标不是"加几个动画"，而是**建立完整动效体系**。每个组件都必须有：进入态 / 悬停态 / 切换态 / 数据变化态。漏一个算未完成。

---

## 1. 全局动效基建（先做这层，所有组件依赖它）

1. **过渡令牌**（规格书 0 节已有 `--dur-fast: .15s / --dur-mid: .3s / --dur-slow: .6s / --ease-out`）——**所有组件必须引用这些变量**，禁止散落硬编码时长。
2. **通用卡片入场**：页面切换后，目标页所有卡片按顺序 stagger 淡入（opacity 0→1 + translateY 12px→0，间隔 40ms，顺序=DOM 顺序），总时长 ≤600ms。
```css
.card-enter { opacity: 0; transform: translateY(12px); transition: opacity var(--dur-mid) var(--ease-out), transform var(--dur-mid) var(--ease-out); }
.card-enter.visible { opacity: 1; transform: translateY(0); }
/* stagger：JS 按索引设置 transition-delay = index * 40ms */
```
3. **hover/focus 微交互**：所有可点击元素（按钮、卡片、列表项、图标）hover 必须有反馈，至少 2 选 1：① 背景色/边框色变化 ② 轻微 transform（translateY(-1px) 或 scale(1.02)），时长 `--dur-fast`。禁止"hover 无任何反应"。
4. **数值变化过渡**：所有进度条、百分比数字、曲线数据更新，必须带动画（进度条 `transition: width .6s var(--ease-out)`；数字用 rAF 插值或 CSS `@property` 计数动画）。
5. **弹层/菜单**：所有 popover/dropdown/弹窗从 `v-if` 硬显隐改为过渡（scale 或 fade + 位置 origin，`--dur-mid`），禁止凭空出现。
6. **滚动条定制**：全局细滚动条（::-webkit-scrollbar 8px，thumb 用 `var(--border)`，hover 用 accent），三屏风格统一走变量。
7. **`prefers-reduced-motion: reduce`**：一、二期已有全局降级，本期新增的动画必须同样降级（全部瞬时完成），禁止漏。

---

## 1.5 悬浮光效系统（本期重点专项）

**原则**：光效是"这个元素活着"的主要信号，三屏各用主题色发光——现在=蓝 `var(--accent)`、过去=金 `#D4AF37`、未来=霓虹青 `#00E5FF`/品红 `#FF00E5`。

**三种光效形态**（按组件层级选用）：

1. **跟随光斑（高级，按钮/卡片强推）**：鼠标移入，一个径向渐变光斑跟随光标，组件底色随之"点亮"。实现基准：
```css
.glow-spot { position: relative; overflow: hidden; isolation: isolate; }
.glow-spot::before {
  content: ''; position: absolute; inset: 0; z-index: -1; opacity: 0;
  background: radial-gradient(220px circle at var(--mx, 50%) var(--my, 50%), var(--accent-soft), transparent 70%);
  transition: opacity var(--dur-fast) var(--ease-out);
}
.glow-spot:hover::before { opacity: 1; }
/* JS：pointermove 时更新 --mx/--my（组件级封装 once） */
```
2. **边缘发光（卡片/面板 hover）**：hover 时 1px 边框变 accent 色 + 外围柔光 `box-shadow: 0 0 16px var(--accent-soft)`，`transition: border-color, box-shadow var(--dur-mid)`。禁止突然出现——必须有过渡。
3. **文字辉光（文字按钮/标题 hover）**：`text-shadow: 0 0 12px var(--accent)` 渐入，`transition: text-shadow var(--dur-fast)`；过去页用金辉，未来页可叠加 glitch 微抖（只对标题，不整段）。

**必须使用光效的组件清单**：顶栏各按钮、三段指示器、登录按钮、tag-item、规划列表项、日历单元格、通知铃铛、绝密开关、三屏切换箭头、MD 工具栏图标。漏一个算未完成。

## 1.6 去框线专项（减法是本期另一主线）

**原则**：能不加框就不加框。**按钮加边框是视觉噪音**——一个按钮如果底色/文字已经表达了它可点击，边框就是多余的。你现在大量 `.btn-ghost`/描边按钮/带边框卡片式按钮全是"为了框而框"。

**必须拆除的框**（对比现状逐项删）：

1. **按钮类**：
   - `.btn-ghost`、`.btn-cyber` 等"无底色+描边"按钮 → 改为**文字按钮**：无边框，hover 用文字辉光（1.5-3 形态）+ 颜色变 accent，按下 `scale(.97)`。**唯一保留描边按钮的例外**：登录页输入框旁边的渐变描边按钮（§2 特例）。
   - 顶栏 icon-button/text-button：无边框（现状已是），补上光效即可。
   - 日历 FullCalendar 的 `.fc-button`：去掉默认白描边，改文字+发光。
2. **列表项**：`买一赠一`式双重线框禁止——列表项已有的 `border-bottom` 分隔线保留一个方向即可，**不得再加整框**（如 `.plan-item`、`.writer`、`.track-list li` 现在都是单线，保持单线别加框）。
3. **卡片**：信息容器卡片保留细分隔线（层级需要），但**卡片内层不要再套卡片**（如面板里再包 panel）；统一用背景色差（`--surface-2`）+ 一个方向的分隔线表达层次，而不是 2px 灰框。
4. **权限/设置相关控件**：`.segmented` 分段控件、select/input 的外框保留（它们是"可编辑"语义），但微调为细线 + 聚焦发光，不加重描边。

**验收规则**：页面上任何"仅仅是装饰"的 1px 边框都是扣分项。"你明明可以就放文字，为什么加个框"——每有一处，算一次未通过。

---

## 2. 登录页：输入框赛博渐变框（硬性需求，按此实现）

现状：普通 input（`rgba(255,255,255,.06)` 底 + 白边框）。不合格。

要求（黑底、渐变框、聚焦点燃）：
1. 输入框外框 = **渐变描边**（不是纯色边框）：用双重背景模拟——
```css
.login-key-input {
  background:
    linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.04)) padding-box,
    linear-gradient(90deg, #00E5FF33, #FF00E566, #00E5FF33) border-box;
  border: 1px solid transparent;
  border-radius: 2px;
}
```
2. **未聚焦态**：渐变边框低饱和、偏灰（`#00E5FF1A, #FF00E540, #00E5FF1A`）。
3. **聚焦态（核心效果）**：点击瞬间，渐变从**中间向两侧点亮**——用背景尺寸动画：渐变初始宽度 0%（中点一个亮点），聚焦时 background-size 从 `0% 100%` 平滑展开到 `100% 100%`（`transition: background-size .45s var(--ease-out)`），会产生"中间点火，向两边烧过去"的效果。
```css
.login-key-input { background-size: 0% 100%, 100% 100%; background-repeat: no-repeat; transition: background-size .45s var(--ease-out), box-shadow .3s; }
.login-key-input:focus { background-size: 100% 100%, 100% 100%; box-shadow: 0 0 18px rgba(0,229,255,.25); }
```
4. 聚焦时边框整体变亮、外发光（chen=青 `#00E5FF` 微光，与 glitch logo 的青红呼应）。
5. placeholder 文字 `#ffffff40`，输入文字白色，光标色青。
6. 登录按钮：与输入框同风格——渐变描边、hover 时发光、按下 scale(.98)。

验收：聚焦输入框能看到"正中亮点向两侧扩展"的动效；未聚焦时框是灰暗的，聚焦后整体点亮。

---

## 3. 时间轴平滑缩放（重点，逐条验收）

现状：`PastView.vue` 用 `spanDays`（14 起步）+ 滚轮 `×0.7 / ×1.4 取整`；`scale` 在 ≤35 / ≤730 / 其余 三档**硬切** day/month/year。滚动全程无过渡，直接重排 DOM。

要求改成**连续平滑缩放**：

1. **spanDays 变连续值**（float 而非 int）：滚轮每次 `spanDays *= Math.pow(1.05, Math.sign(deltaY) * -1)` 之类连续因子，不取整。滚一下可能变 13.3、滚两下 12.7——**允许小数，这就是你要的"不规则天数"**。范围：最小 3 天，最大=注册日到今天全部天数。
2. **UI 渲染用 rAF 驱动逼近目标值**：维护 `displayDays`（实际渲染用）和 `targetDays`（滚轮改），每帧 `displayDays += (targetDays - displayDays) * 0.12`，直到差值 <0.01。这样滚轮滚动时时间轴**平滑缩放**而不是瞬跳。刻度、日期条、曲线全部跟随 `displayDays` 重算。
3. **天→月→年刻度平滑过渡**：不再按阈值硬切，而按"每像素对应天数"（pxPerDay = 轴高度 / displayDays）决定标注密度：
   - pxPerDay ≥ 18px：天级（每行一天，标"xx日"，跨月带月份）
   - 6px ≤ pxPerDay < 18px：过渡带——仍逐日渲染但标签每 N 天一个；N 随 pxPerDay **连续取整**（如 pxPerDay 8px → 每 2 天一个标签），月份行首加粗
   - pxPerDay < 6px：月级（每月一行标月份），更小 pxPerDay < 1px 时年级
   - **刻度标签的增删带 opacity 过渡**（新标签淡入、消失标签淡出，150ms），禁止标签瞬间出现/消失
4. **日期数字同样平滑**：时间轴行高 = 固定 pxPerDay × 天数，缩放时行高渐变（由 2 的 displayDays 驱动天然平滑），禁止行高突变。
5. 里程碑勋章、今日标记随行移动，位置连续。

验收：快速滚轮 5 格，观察时间轴**连续缩小**（能看到 30→24→19→15→12 的中途状态，无跳变）；缩到月级时是"天行逐渐变密变矮最终合并成月"，不是"啪一下变成月"。

---

## 4. 全组件动效核查表（逐项补齐，缺一项算未完成）

| 组件 | 现状 | 要求 |
|------|------|------|
| **顶栏导航** | hover 仅文字变色 | hover 加背景渐变淡入 + 当前屏指示条 slide 到目标（指示条用 transform translateX 过渡，不是瞬间换位置） |
| **三屏切换** | 已有 VT 方向动画 | ① 目标页内容 stagger 入场（见 §1.2）② 未来页入场时扫描线/网格有轻微"点亮"过渡 ③ 过去页金辉有 fade 过渡 |
| **登录→面板过渡** | 问候语后硬切 | 现在页入场 anim（scale 1.03→1 + opacity 0→1，600ms），与二期的 greeting 衔接 |
| **时间区时钟** | 秒数字直接变 | 秒更新用 0.2s 数字淡入（或 opacity pulse），禁止无过渡跳秒；日期/农历切换加 fade |
| **身体/心情曲线（MetricLine）** | 数据更新直接重绘 polyline，无动画 | 新数据点时 polyline 有 500ms 形态过渡（可用 CSS `transition: d` 不兼容 → 用 JS 在两个 path 间 lerp，或至少整条线淡入）；hover 点高亮 |
| **tag 选中** | 边框/背景突变 | 选中加 scale(1.05) 微弹 + accent 光晕；切换选中时旧项缩回（transition: all var(--dur-fast)） |
| **横幅轮播** | `activePlan` 直接换 | 切换时卡片 slide-fade（旧卡向左淡出，新卡从右滑入，300ms），进度指示点随动 |
| **待办勾选** | done 直接加删除线 | 勾选瞬间：删除线从左侧画到右侧（`text-decoration` 不可动画 → 用 `background linear-gradient` 宽度 0→100% 模拟划线），checkbox 打勾有 pop 反馈 |
| **进度条（双进度/横幅/规划）** | width 突变 | 全部 `transition: width .6s var(--ease-out)`；数值文字同步插值 |
| **过去页卡片选中** | 选中即高亮 | 选中卡片 200ms scale(1.01) + 金边光晕淡入；非选中缩回。勋章出现带 300ms 弹入（scale .6→1 + fade） |
| **未来页规划列表** | hover box-shadow 突变 | hover 300ms 过渡发光（transition box-shadow）；选中项 clip-path 面板有 200ms 点亮 |
| **未来页缩放日历** | 缩放硬切单元格 | 单元格宽度/数量变化带过渡：懒做法=容器 `transition: transform` 视觉放大（终版按 §3 连续缩放）；至少消除"啪一下重排" |
| **通知中心弹层** | v-if 硬显隐 | 弹层 fade + translateY(8px)→0（200ms，origin 右上）；红点出现有 pop |
| **音乐按钮** | 播放/停止文字切换 | 图标状态切换有 150ms 过渡（播放三角→停止方块的 morph 或 cross-fade）；当前曲目滚动显示 |
| **设置页 tab** | 内容硬切 | tab 切换内容 fade-slide（100ms）；分段控件选中滑块有 slide 过渡 |
| **绝密模式** | 主题色突变 | 整页颜色过渡（`transition: background var(--dur-slow), color` 等，所有色变量迁移至少 400ms），变红是渐入不是瞬变 |
| **空状态** | 一行灰字 | 图标（简单 SVG）+ 文案，整体 300ms fade 入场 |

---

## 5. MD 编辑器（承接二期决策落地）

1. **图片点击放大**：自研全屏 overlay（portal 挂 body），点击预览中图片 → 主题色遮罩 + 图片居中 + 关闭按钮（Esc/点击遮罩）+ 滚轮缩放 + 拖拽平移。**弃用 medium-zoom**。三屏主题各一套遮罩色（现=蓝黑、过去=暖棕暗角、未来=霓虹边）。`prefers-reduced-motion` 时无缩放过渡。
2. **删除备份审计**：保留 CodeMirror 原生 undo；新增持久化层——监听编辑器内容，diff 出被删段落，**单次删除 ≥50 字**自动存快照（localStorage + 可同步服务器），提供"查看/恢复删除段落"入口（如工具栏垃圾桶按钮弹出列表）。**禁止重写 CM undo**。
3. **保留功能**：图片、链接、数学公式(katex)、图表(mermaid) 工具栏项全部保留（为未来协作平台留口），不裁剪。

---

## 6. 文案黑名单复审（三期新增）

- 禁止在动效相关处写任何装饰性文字（如"正在平滑缩放…"等），加载/过渡一律用视觉反馈（进度、骨架、透明度），不加文案。
- 一、二期黑名单继续生效。

---

## 7. 验收清单（全部打勾才算通过）

- [x] 全局：卡片 stagger 入场、hover 微交互、数值过渡、弹层过渡、细滚动条，全部落地且走令牌变量
- [x] 悬浮光效：顶栏/按钮/tag/规划列表/日历格/铃铛/绝密开关/切换箭头 全部有主题色光效（跟随光斑或边缘发光或文字辉光，三选一达标），无光效 = 未完成
- [x] 去框线：`.btn-ghost`/描边按钮全部改文字按钮；无装饰性 1px 框；卡片内不套卡片；列表项只保留单方向分隔线
- [x] 登录输入框：黑底赛博渐变框，聚焦"中间向两侧点亮"动效符合 §2
- [x] 时间轴：连续缩放（小数天数）、rAF 平滑、刻度密度渐变+淡入淡出，无任何"跳变/硬切"
- [x] 三条曲线（身体/心情/过去趋势）数据变化有形态过渡
- [x] 待办勾选划线动画、进度条 0.6s 过渡、横幅轮播 slide-fade
- [x] 通知中心/设置 tab/绝密模式 都有过渡，绝密变红 ≥400ms 渐变
- [x] 图片放大自研 overlay 可用；删除 ≥50 字有备份可恢复；公式/图表工具栏保留
- [x] 一、二期已达标项零回退
- [x] `prefers-reduced-motion` 下本期全部动画瞬时完成
- [x] 文案黑名单零违例

**交付判定**：§2 登录框、§3 时间轴为硬性项，这两项不过 = 整体不通过。其余每缺一项扣一项，全部通过才可 commit 声称"三期完成"。

**最后一句**：不要用"功能能用了"搪塞——本期唯一的验收标准是**打开页面能明显感觉到"这个网站有生命"**。所有过渡加起来如果少于 20 处，你一定漏了。


---

## 8. 三期验收记录（2026-09-05）

执行分支 main，逐项提交（`git log --oneline` 可见 §1 / §1.5 / §2 / §5 / §4 现在页 / §3 时间轴 / §4 未来页 / §4 设置页）。

- 动效资产：样式层共 128 处 transition/animation 声明、25 组 keyframes，全部引用 `--dur-*` / `--ease-*` 令牌；`prefers-reduced-motion` 下 CSS 全量 .001ms，JS 侧 rAF 逼近/数值插值/stagger 直接落到目标值。
- 去框线：装饰性 1px 边框仅保留在"可编辑"语义控件（输入框、编辑器外框）与登录页渐变描边（§2 特例）；卡片/弹层/横幅/标签/日历/列表项统一为背景色差 + 单方向分隔线。
- 硬性项：§2 登录渐变框（mask 合成 1px 环，聚焦时亮环从正中向两侧展开 .45s）；§3 时间轴（浮点天数 + rAF 逼近 + 按 pxPerDay 连续决定标签密度 + 150ms 淡入淡出，无阈值硬切）。
- 验收方式：本地 Chrome DevTools 协议无头驱动逐屏截图（登录序列、悬浮、弹层、绝密/亮暗切换、时间轴多帧缩放、未来页缩放、删除备份快照与恢复）。

### 本期顺带修正的阻断问题（非前端范围，已单独提交）

1. 后端 `auth.KeyLogin` 在单连接 SQLite 池下持游标做嵌套查询/写入 → 书写者/阅读者密钥登录永久阻塞；已改为先读完候选再校验。**本机无 Go 工具链，未本地编译，需服务端构建确认；`product/bin/cyberlife.exe` 仍是旧构建。**
2. 后端 `history.visibleMilestoneCount` 未对 NULL 的 visibility_preset_id 做 COALESCE → 任一带里程碑的日期使 /history 整体报错；已补。
3. 后端 Actor 结构体无 json 标签（返回 PascalCase），前端原按 snake_case 读取，导致书写者被当作阅读者渲染；前端已做字段归一化。

### 接口尚不支持、前端已做降级的项

- 问候语昵称：/auth/me 与密钥登录均未返回昵称，暂显示"书写者/阅读者"。
- 时间轴"注册日"下限：接口未提供人生创建时间，轴下限回退为今天之前十年。
- 规划简介编辑：无更新规划接口，编辑模式只能标记进度；封面/图标/文件列表无字段，未放占位。
- 待办只能为"今天"创建（/now/tasks 无日期参数），未来页点选其它日期只展示。
- 日记绝密层：接口只有单篇日记的 secret 标志，绝密模式下心情/身体自动带绝密标记，日记不自动翻转可见性。
