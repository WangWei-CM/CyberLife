# Cyberlife 部署、备份与开源组件说明

## 1. 目录约定

```text
data/
  global.db
  lives/{life_id}/YYYY-MM.db
uploads/{life_id}/
backups/{life_id}/
```

运行数据不得放入 Git 仓库。使用 `.env` 或系统服务环境变量提供配置，至少设置：

```text
CYBERLIFE_ADMIN_PASSWORD=<长且唯一的管理员密码>
CYBERLIFE_DATA_DIR=/srv/cyberlife/data
CYBERLIFE_SECURE_COOKIES=true
```

## 2. 反向代理

Go 服务仅监听 `127.0.0.1:8080`，由 Nginx/Caddy 终止 HTTPS。代理必须：

- 转发 `X-Forwarded-Proto` 和 `X-Request-ID`；
- 限制上传请求体不超过 20MB；
- 开启 CSP、HSTS（确认全站 HTTPS 后）和基础限流；
- 不将 `data/`、`uploads/` 暴露为静态目录；
- 仅允许 Web 前端来源访问带 Cookie 的 API。

## 3. SQLite 备份

1. 停止或协调对应 Life 的短写事务。
2. 使用 SQLite backup API 或一致性快照，不直接复制正在写入的 WAL 文件。
3. 同时保存 `global.db`、全部月库、设置文件和附件。
4. 生成 `manifest.json`，包含 schema 版本、Life ID、文件相对路径、大小和 SHA-256。
5. 备份写入临时目录，校验完成后原子移动到 `backups/{life_id}`。
6. 恢复到隔离目录，执行 SQLite integrity_check、foreign_key_check 和附件引用检查。
7. 恢复演练成功后才允许覆盖式导入。

恢复失败不得覆盖原数据。备份目录应由应用账户可写、其他普通账户不可写。

## 4. 发布前安全检查

- 默认密码和示例密钥不得用于生产。
- 日志不得出现原始密钥、Cookie、Authorization、Markdown 正文或 LLM API key。
- 上传文件使用服务端 UUID 文件名，禁止使用用户输入拼接路径。
- 对上传内容执行大小、扩展名、MIME 和文件头校验；下载必须先通过资源 ACL。
- Reader 的列表、统计、趋势、时间轴、评论和附件下载均不得暴露无权资源存在性。
- 修改 ReaderKey 后验证既有会话立即失效。
- 导入包必须验证 Life ID、版本、校验和路径安全。

## 5. 开源组件及许可证

当前 Web 依赖采用 MIT 许可的开源项目：

- [FullCalendar Vue 3](https://github.com/fullcalendar/fullcalendar-vue)：日历 Vue 3 适配器；核心包、月视图和日期交互插件来自 FullCalendar。
- [md-editor-v3](https://github.com/imzbf/md-editor-v3)：Vue 3 Markdown 编辑器，提供编辑/预览和工具栏。
- 待办的重复规则、提醒和交互参考 [MoliTodo](https://github.com/gusibi/MoliTodo) 的公开实现思路；本项目不复制其桌面应用代码，任务数据仍由 Cyberlife API 持久化。

发布包应随前端依赖生成对应的 license 清单，并保留各项目版权声明。依赖升级前重新确认许可证和 Vue/Vite/TypeScript 兼容性。

## 6. 备份 API 契约

服务端应以后台任务执行导出，不让 HTTP 请求持续占用连接：

```text
POST /api/v1/backups                 # Writer 创建本人人生导出任务
GET  /api/v1/backups/:id             # 查询状态、错误和 manifest 摘要
POST /api/v1/admin/backups/:lifeID   # Admin 导出指定人生
```

导出包必须包含 `manifest.json`、global 身份索引、所有月库、settings 和附件；每个文件记录相对路径、大小、SHA-256 与 schema 版本。导入前先写入隔离目录，完成校验后再原子切换。

## 7. Android

Android 客户端是原生 Kotlin 薄客户端，不嵌入 WebView。当前工程位于 `android/`，目标职责：

- WorkManager 网络可用时轮询提醒；
- Android 通知渠道和事件去重；
- SpeechRecognizer 语音转文字；
- LLM 输出先经 JSON Schema 校验，再由用户确认写入任务。

Android 构建与设备测试需要 Android SDK/Gradle 环境；当前会话只要求完成源码骨架，未执行 Android 构建。
