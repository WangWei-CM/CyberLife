# Cyberlife 前端整改军令状 · 二期（v2.0）

> 命令对象：前端实现 AI。
> 依据：`策划书.md`、`视觉规格书.md`、`前端整改军令状.md`（一期）、本文件。本文件为**增量**，一期要求继续有效，本文件只列"新增+回归修复"，如有冲突以本文件为准。

---

## 0. 一期验收结论（先看这个）

你上一版（`d263191 rebuild Cyberlife visual experience`）相比第一版是**质的进步**，以下项目已达标，**禁止再改坏**：

- 设计令牌三套主题（`.theme-now/past/future` × light/dark）全量落地，三页背景色正确区分
- 登录页：纯黑、glitch logo、无标语、无管理员 Tab、动画序列（离开 0.5s → 问候 2s → 进场）
- 音乐真实 `<audio>` 播放、三页断点续播（index+time）、三模式（list/random/single）
- MetricLine 真 SVG 折线（替换 CSS 柱条）
- 未来页自研缩放日历（抛弃 FullCalendar）、四边形规划卡 clip-path
- 过去页滚轮缩放、指针拖拽平移、今天在底部、勋章 ✦
- 现在页两栏可拖拽分隔、真农历（Intl chinese）、秒小字体
- 顶栏四方向拖拽、绝密模式整屏变红、主题三档真生效、prefers-reduced-motion 全局降级

**以上任何一项在后续提交中回退，视为严重事故。**

---

## 1. P0 回归修复（必须立即修）

### 1.1 [x] 【回归】管理员登录入口丢失
- 现象：一期之后 `LoginView` 删掉了"人生密钥/管理员"切换，但后台 `api.adminLogin`、`AdminView.vue` 都在，`App.vue` 仍按 `actor.type==='admin'` 渲染管理面板——**现在没有任何 UI 能登录为管理员**，管理功能不可达。
- 要求：恢复 `/admin` 独立入口（路由或独立页面）。按需求：管理员入口**不在主登录页出现**，是独立路径 `/admin`。
- 验收：访问 `/admin` 出现独立登录表单；输管理员密码后进入 AdminView；主登录页不受影响。

### 1.2 [x] 【假实现】进行中规划横幅没有轮播
- 现象：`NowView.vue` 里 `plans.slice(0, 1)` 只渲染第一张，无轮播、无间隔配置。
- 要求：实现横向轮播（CSS `scroll-snap` 或 JS 定时器），默认 6 秒切换，可暂停、可手动切换，多张时显示进度指示点。判断"进行中"：开始≤今天≤截止 且 进度<100%。
- 验收：有 2 张以上进行中规划时能看到自动轮播，间隔可配置。

### 1.3 [x] 【假实现】音乐上传仍是本地 blob，没进服务器
- 现象：`SettingsView.addTrack` 用 `URL.createObjectURL(file)` 存 localStorage——刷新/换设备/换浏览器歌单即失效，与需求"上传音频"（服务器持久化）不符。
- 要求：调服务器上传接口（参考日记附件上传模式），音频存服务器，歌单记录返回的 URL。浏览器端 blob 方案**禁用**。
- 验收：上传后刷新页面仍在；另一台设备登录同一人生仍能播放同一歌单。

---

## 2. P1 规格未达（按需求补齐）

### 2.1 [x] 过去页时间轴：缺"月/年级刻度"分级
- 需求（规格书 2.4）：滚轮向上放大→天级；向下缩小→**月份刻度、再缩小→年份刻度**；天级只标"xx日"，跨月带月份。
- 现状：`zoom` 只在 3~365 天之间滑动的**每日一行按钮**，没有"缩到月/年时刻度变成月份/年份、一天不再占一行"的行为。
- 要求：
  - 天级（放大）：刻度按天，标"xx日"，跨月那天带"9月30日"；轴长=屏幕上 3~5 天
  - 月级：刻度按月，标月份
  - 年级：刻度按年，标年份
  - 三个级别由缩放连续过渡，点击轴上任意位置仍精确选中某一天（即使刻度没显示）
- 验收：滚轮缩小到一定程度后，轴上的行从"每天一行"变成"每月一行/每年一行"。

### 2.2 [x] 过去页时间轴视觉：还没到"人生长卷"标准
- 现状：仍是按钮列表 + 右侧边线，缺规格书 2.4 的金线轨道、今日标记（金色横线）、里程碑金色勋章节点（现在只有 ✦ 字符）。
- 要求：轨道有连续金色主线；今天位置有金色标记线；里程碑在对应日期有圆形金徽章节点（带微光）；选中日高亮为金线贯穿。
- 验收：视觉上是一个"可缩放的编年史卷轴"，不是普通列表。

### 2.3 [x] 三屏切换动画无方向定制
- 现状：`startViewTransition` 已接入，但所有方向都是默认 cross-fade（`::view-transition` 仅统一时长）。
- 要求（规格书 §5）：
  - 现在→未来：旧屏 glitch 切片消失 + 新屏霓虹光从右侧扫入（`clip-path` 横向展开）
  - 现在→过去：暖色光晕过渡（`filter: sepia` 淡出 + 淡入）
  - 未来→过去/现在：反向
- 做法：给 `::view-transition-old(root)/new(root)` 按当前/目标主题写不同动画；`<html>` 或目标页根节点加 `data-transition="to-past|to-future|to-now"`，CSS 按属性分支。
- 验收：三对方向各自有可感知的区别，且 `prefers-reduced-motion` 时全部静默。

### 2.4 [x] 音乐：需求"三页独立歌单 + 默认音频"
- 现状：三个页面共用一套 localStorage 键，切页曲目跟随但**没有"三页各自独立列表"的直观编辑**（有 page 切换按钮，可以），缺**系统内置默认音频**（三页默认不同）。
- 要求：内置默认音频（可由 Web Audio 合成或内置资源，格式不限）；三个页面默认歌单不同；用户可删除默认音频换成自己的。
- 验收：不传任何音频，三页也能各自播放默认音频；默认音频可被删除。

---

## 3. P2 功能缺口（本期内如有余力做，不做也不卡上线，但提交时必须声明）

- [ ] 延期：规划详情缺封面图/图标/文件列表（后端 Plan 无这些字段时先留 UI 占位）
- [ ] 延期：通知中心点击跳转（评论→该日记、规划→详情）未实现
- [ ] 延期：ReaderView 视觉与现在页完全同构，未按阅读者视角打磨
- [ ] 延期：Android 端仍是占位（PollWorker/VoiceTaskFlow），本期可视作"未开始"

---

## 4. 新增红线（叠加在一期之上）

1. **禁止 `slice(0,1)` 之类的"假装有数据"写法**：轮播必须多张可切换。
2. **禁止把用户数据存 localStorage 冒充持久化**：音频、歌单、头像、配置等必须走服务器（除非需求明确说本地）。
3. **禁止删除已实现的管理员/阅读者入口**去"简化"界面——功能入口和视觉美化是两件事。
4. **禁止在缩放类交互上用"数字输入/下拉选择"替代滚轮/拖拽手势**（登录已排除）。
5. 一期黑名单继续有效：无意义展示语段（`PHASE 1`/`CHRONICLE`/`LOADING` 等）一旦再出现，按未通过处理。

---

## 5. 二期验收清单

- [x] `/admin` 独立入口可登录管理员，主登录页纯净
- [x] 进行中规划横幅多张自动轮播，间隔可配置
- [x] 音频上传进服务器，刷新/换设备歌单仍在
- [x] 过去页时间轴三级刻度（天/月/年），点击仍精确到日
- [x] 过去页时间轴具"编年史卷轴"视觉（金线/今日标记/勋章节点）
- [x] 三屏切换三对方向有定制动画
- [x] 三页各自默认音频，可删除替换
- [x] 一期达标项无回退
- [x] 文案黑名单零违例

**交付判定**：P0 三项不完成 = 不通过；P1 四项不完成 = 不通过；P2 可声明延期。不要用"功能已通"搪塞，也不要为通过检查而删功能。
