# Cyberlife 前端整改军令状（v2.0）

> 命令对象：前端实现 AI。
> 现状判定：功能壳可用，视觉 0 分，文案满屏装饰，登录页未按需求实现。必须推倒重做前端视觉层。
> 依据文档（必须全文阅读并逐条落实）：`策划书.md`、`视觉规格书.md`、本文件。
> 本文件优先级最高；与前面文档冲突时，以本文件为准。

---

## 0. 三条铁律

1. **严禁添加任何无意义的展示语段**。页面上的每一个字都必须承载真实信息。现存的 `CYBERLIFE / PHASE 1`、`NOW / LOADING`、`PAST / CHRONICLE`、`FUTURE / PLANNING SYSTEM`、`READER / FILTERED VIEW`、`SYSTEM / SETTINGS`、`ADMIN CONSOLE`、`正在恢复会话…`、`以密钥进入一段人生。` 等装饰性文案，**一律删除，一个都不许留**。详见第 7 节清单。
2. **视觉效果必须达到《视觉规格书.md》的标准**，不得以"功能优先"为由降级。留白、间距、字体、动效是需求，不是可选项。
3. **动效是刚需**。登录动画序列、三屏切换、卡片入场都必须实现，且必须带 `prefers-reduced-motion` 降级。

---

## 0.5 风格词汇具象词典（重要：抽象词一律按本词典执行）

你的历史实现把"大气"理解成"堆大字号 + 加装饰英文"，这是错的。所有抽象风格词按下面**可逐项验收**的标准执行：

| 词汇 | 具象可落实的标准（验收时逐条打勾） |
|------|------|
| **大气** | 桌面端页面四周留白 ≥32px；卡片间距 ≥16px；单屏主色不超过 2 种（强调色 1 种 + 语义色）；无装饰性元素（无无意义的线框、无多余图标、无英文眉题）；内容区居中且最大宽度 ≤1400px |
| **干净** | 每个卡片只做一件事；卡片内信息密度低（正文行高 ≥1.6，卡片 padding ≥16px）；无杂色、无渐变滥用、无重复边框（一处边框能表达层级就不要第二处） |
| **克制** | 强调色只出现在"可交互/当前状态/关键数据"上，其余全是中性色（text/dim/faint/border）；禁止把强调色铺满按钮以外的大面积；动效时长 0.15~0.6s，禁止慢速飘移 |
| **高级感/艺术感** | 层级只用 4 种手段表达：字号、字重(400/600/700)、颜色(dim/faint)、留白。禁止用"加边框框起来"表达层级；hover/focus 有 0.15s 过渡反馈；字体必须按令牌（无衬线/衬线/等宽各司其职） |
| **留白** | 视觉上"东西少到你觉得太空"是对的；卡片与卡片之间、文字与卡片边缘之间、大标题与正文之间，宁可多留不许多塞 |
| **史诗感/厚重**（过去页） | 具体=背景四层（底色/暗角/胶片颗粒/金辉）；标题衬线体；金色只用于大标题/里程碑/分隔线；正文用暖象牙 #F2E5C9 而非纯白；无任何霓虹/高饱和元素 |
| **赛博朋克感/锐利**（未来页） | 具体=零圆角；平行四边形面板（75°竖边）；四角 L 形角标；扫描线+网格背景；发光代替阴影；等宽字体；文案全大写宽字距。**没有这些具体元素就不叫赛博朋克** |
| **丝滑**（动画） | GPU 属性优先（transform/opacity/filter）；时长与缓动按令牌；页面切换用 View Transitions；禁止元素"跳变"（位置突变、尺寸突变必须加过渡） |
| **现代化**（现在页） | 具体=扁平方角（≤4px 圆角）；细边框 1px + 轻阴影；蓝为主题色；无毛玻璃、无渐变、无立体感；信息层级一行一行排清楚 |

**判定规则**：如果某个页面给一个不懂设计的人看，他说"挺干净的"→ 合格；说"挺酷的/挺有设计感"→ 合格；说"有点乱/花里胡哨/字好多"→ 不合格，重做。

---

## 1. 全局设计语言

- 严格执行《视觉规格书.md》第 0 节设计令牌：三套主题类 `.theme-now / .theme-past / .theme-future`，亮暗双份，颜色一律走 CSS 变量，**禁止在组件里散落硬编码颜色**。
- **三页背景色必须各不相同**（这是硬性视觉区分，禁止互相抄）：

| 页面 | 亮色背景 | 暗色背景 | 来源 |
|------|---------|---------|------|
| 现在 | `#F5F8FF`（蓝白） | `#0B1220`（蓝黑） | 规格书令牌 `--bg` |
| 过去 | `#221B12`（深栗褐） | `#1A1510`（墨棕） | 规格书令牌 `--bg` |
| 未来 | `#0A0A12`（深空黑） | `#07070D`（近纯黑） | 规格书令牌 `--bg` |

  违反表现（一票否决）：把三个页面做成同一个深色底 + 不同强调色——这不算"背景不同"。过去页必须明显偏暖棕、未来页必须纯黑带蓝紫调、现在页亮色必须是浅蓝白。

- 字号体系：标题 20/28px（700，**不得再大**），正文 14px，辅助 12px（dim）。禁止超大字号吼人。
- 间距体系：卡片内边距 16~24px，块间距 16px，页面左右留白 ≥32px（桌面）。
- 圆角：现在 4px / 过去 8px / 未来 0px。
- 所有按钮/输入框必须有 focus 态（accent 边框或发光），hover 态有轻微反馈。
- 空状态必须有设计：居中的图标/图形 + 一句克制的中文说明，**禁止**一行灰色小字打发。

---

## 2. 登录页（严格按需求，一点不许改）

### 2.1 视觉
- 背景：**纯黑** `#000000`。无背景图、无渐变、无网格、无任何装饰图形。只允许以下三个元素存在：
  1. 故障风 logo（Cyberlife 文字标，带 glitch RGB 分离闪烁）
  2. 密钥输入框
  3. 登录按钮（或回车提交）
- **禁止**：mode 切换按钮（人生密钥/管理员分 Tab）——管理员入口独立走 `/admin` 路由，登录页不出现。
- **禁止**：`PHASE 1`、`以密钥进入一段人生。` 等任何标语、眉题、副文案。一个多余的字都不许有。
- logo 全大写 `CYBERLIFE`，等宽/科技感字体，glitch 效果参考规格书 3.9（红/青偏移层 + 闪烁动画，2s 周期，`prefers-reduced-motion` 下静态 RGB 偏移）。
- 密钥输入框：透明玻璃质感（半透明底+细边框），宽度 ≤380px，居中；placeholder 只写"粘贴主密钥或阅读密钥"。

### 2.2 动画序列（输入正确密钥提交后，严格按顺序）
1. **logo 放大淡出**：`scale(1.15)` + `opacity → 0`，0.5s
2. **输入框透明**：`opacity → 0`，0.5s
3. **问候语出现**："早上好 / 上午好 / 中午好 / 下午好 / 晚上好，{昵称}"（书写者显示书写者昵称，阅读者显示密钥昵称）。opacity 0→1（0.5s）→停留 1s→1→0（0.5s）。淡入淡出 0.5s、停留 1s，时长做成可配置项。
4. **现在页入场**：以**页面正中为原点** `scale(1.03) + opacity(0)` 淡入到正常，0.6s。
- 四步必须衔接丝滑（`requestAnimationFrame` 串场或 CSS 关键帧链），禁止硬切、禁止闪烁。
- 降级：`prefers-reduced-motion` 时全部瞬间切换。

---

## 3. 现在页（Now）— 蓝白/蓝黑扁平

- 背景：亮 `#F5F8FF` / 暗 `#0B1220`（令牌，见第 1 节表）。
- 严格按规格书 1.1 两栏密铺：左栏（时间区→身体卡片→心情卡片→日记）+ 右栏日历，分隔线可拖拽。
- **左上角时间区**：日期（28px/700）｜ 星期 ｜ 农历 ｜ 北京时间到秒（**秒数字体更小**、等宽防跳动）。真实显示，禁止 `toLocaleString` 糊弄。
- **身体卡片**（规格书 1.4）：左=近 7 日身体曲线（折线图，不标数值），右=滑杆 0-100 + 备注 + 提交。
- **心情卡片**（规格书 1.5）：左=今日曲线（按分钟散点连线），右=tag 网格多选（emoji+名称，6 列）+ 备注 + 提交。曲线**禁止用 CSS 柱条冒充**，用 ECharts 或手绘 SVG 折线。
- 日记：`md-editor-v3`，自动保存 700ms 防抖可保留，保存提示用克制文字（"已保存 14:32"），禁止"保存中…"闪不停。
- 待办在右栏日历今日视图（日历即待办）。左栏不单独放待办。
- 顶部进行中规划横幅轮播（规格书 1.10）：规划名 + 双进度条，可点击跳未来页详情。**只展示不可编辑**。
- 禁止 `NOW / LOADING` 眉题、`此刻` 大标题横幅。

---

## 4. 过去页（Past）— 深金史诗调

- 背景：亮 `#221B12` / 暗 `#1A1510`（明显暖棕，与另外两页区分）。
- 严格执行规格书 2.1 四层背景：深栗褐底色 + 电影暗角 + SVG noise 胶片颗粒 + 顶部金色柔光脉动。
- **时间轴**（规格书 2.4）：竖向、默认左侧、可拖放到上下右；范围=注册日→今天；滚轮缩放（向上放大到天级、最大时屏幕内 3~5 天，向下缩到月/年级）；左键拖动平移；点击精确选中某天；**竖置今天在底部**；里程碑金色勋章节点；今日金色标记线。**禁止 range 滑块+左右按钮糊弄缩放**，必须是滚轮+拖拽原生交互。
- **曲线组件**（规格书 2.6）：两个折线统计图（心情+身体），随范围联动，不标数值，暖金配色。禁止 CSS 柱条。
- 选中日内容：日记 ｜ 该日日程（任务+里程碑+评论）左右对半开（规格书 2.7）。日记渲染 markdown，禁止 `<pre>` 裸文本。
- 风格：**深金史诗调**（规格书 5.12）。标题衬线、鎏金 `#D4AF37`、暖象牙文字 `#F2E5C9`、卡片展柜感、里程碑磨损徽章（规格书 2.3）。禁止做成普通深色页。
- 禁止 `PAST / CHRONICLE` 眉题、`过去` 大字标题横幅。

---

## 5. 未来页（Future）— 赛博朋克

- 背景：亮 `#0A0A12` / 暗 `#07070D`（纯黑带蓝紫调，与过去页暖棕截然不同）。
- 严格执行规格书 5.9：平行四边形面板（75°竖边，规格书 3.1 两套方案任选）、四角 L 形角标（3.2）、扫描线叠加（3.3，`pointer-events:none`）、网格背景（3.4）、霓虹发光代替阴影（3.5）、玻璃拟态（3.6）、等宽字体全大写（3.8）。**没有这些具体元素就不算赛博朋克，禁止做成"普通深色+青色标题"。**
- 布局（规格书 3.13）：上部左右 3:7（左=规划列表按截止日期升序，右=详情，分隔线可拖拽），下部=自研可缩放待办日历（3.14：默认 7 天、滚轮缩放至 14/30/半年/1年/5年/10年/30年/100年、点击某天添加待办）。
- **禁止用 FullCalendar 冒充**：未来页日历必须是自研缩放视图。FullCalendar 只允许出现在现在页右栏。
- 规划详情：名称/起止日期/封面图/图标/简介（markdown 渲染）/文件列表/时间进度条（青 `#00FFF5`）+计划进度条（品红 `#FF00FF`）/右上角"编辑模式"按钮（编辑态可改简介 MD、填完成百分比）。
- 进行中判定：开始≤今天≤截止 且 进度<100%，按截止日期升序。
- 禁止 `FUTURE / PLANNING SYSTEM` 眉题、`未来` 大字标题横幅。

---

## 6. 通用组件

- **导航**：顶栏中央"过去·现在·未来"三段指示器 + 两侧翻屏箭头（不是 Tab 按钮）；右侧=音乐播放/停止、亮暗切换、设置、通知铃铛、绝密模式开关（书写者）、身份昵称、登出。顶栏可拖到左/右/下边缘（书写者保存布局）。
- **三屏切换**：View Transitions API（规格书第 5 节），现在→未来 glitch 故障扫入，现在→过去暖色光晕，全部带 `prefers-reduced-motion` 降级。**禁止无过渡硬切。**
- **绝密模式**：书写者开关，开启后整屏主题变红（规格书 1.9 `.secret-mode` 变量覆盖），日记切绝密层编辑器，心情/身体自动带绝密标签。
- **音乐**：必须真实播放。`<audio>` + 三页独立歌单 + 切页换列表 + 切回断点续播 + 三模式（列表循环/随机/单曲）+ 音量百分比（默认 70%）。**禁止点了没声音。**
- **通知中心**：铃铛 + 红点未读数 + 弹层（时间倒序、未读高亮、点击跳转）。
- **设置页**：tab 分块（歌单/权限预设/主题/密钥管理/备份导出/登出）。主题三档：始终黑/始终白/日出日落，**必须真实生效**（不能只写 dataset 不写 CSS）。
- **布局可调性**：卡片顺序拖拽、卡片高度拖拽、左右栏宽度拖拽、所有分隔线可拖拽（规格书 5.8）。**禁止静态 grid 交差。**

---

## 7. 文案规范（无意义展示语段黑名单）

| 禁止（现存于代码中） | 处理 |
|---|---|
| `CYBERLIFE / PHASE 1` 等眉题 | 删 |
| `NOW / LOADING` | 删，加载态用克制加载指示 |
| `PAST / CHRONICLE` | 删 |
| `FUTURE / PLANNING SYSTEM` | 删 |
| `READER / FILTERED VIEW` | 删 |
| `SYSTEM / SETTINGS` | 删 |
| `ADMIN CONSOLE` | 删 |
| `以密钥进入一段人生。` | 删 |
| `正在恢复会话…` | 换无文字进度指示或 200ms 内静默恢复 |
| `正在读取并过滤历史内容…` | 换骨架屏/加载指示 |
| `{{xxx}} 项未完成` 等计数眉题 | 非必要删，必要改内联小字 |
| 任何仿系统标签/营销腔/感叹号 | 删 |

**允许存在的文案**：真实数据（日期、昵称、任务标题、进度百分比）、真实操作提示（"保存""删除""作废"）、克制的空状态说明、"已保存 14:32"。

**原则**：页面上的每一个字都必须有信息价值。装饰性英文、仿系统标签、感叹号、营销腔——全部是扣分项。

---

## 8. 验收清单（交付前逐项自检）

- [ ] 登录页：纯黑、只有 logo+输入框+按钮，动画四步完整丝滑，无任何标语
- [ ] 三页背景色各不相同（现在蓝白/蓝黑、过去暖棕、未来纯黑带蓝紫调），无硬编码颜色
- [ ] 现在页两栏布局+时间区（含农历和秒）+真实曲线+横幅轮播
- [ ] 过去页四层背景+滚轮缩放时间轴+金色勋章+双折线图+日记/日程对半开
- [ ] 未来页平行四边形+角标+扫描线+玻璃拟态+自研缩放日历+规划编辑模式
- [ ] 三屏切换有 View Transitions 动效
- [ ] 音乐真实播放、切页换歌单、断点续播、三模式
- [ ] 主题三档真实生效
- [ ] 所有拖拽（卡片/分隔线/顶栏）可用
- [ ] 全文案过第 7 节黑名单，零装饰语段
- [ ] 全部动画带 `prefers-reduced-motion` 降级
- [ ] 对照 0.5 词典逐项自评：不满足"干净/克制/大气"的页面不合格

**做不到以上任何一项，都不算完成。不要用"功能已通"搪塞——视觉和动效本身就是需求。**
