# UI 视觉系统规格书

> 版本 v1.0 ｜ 2026-09-03
> 用途：为 AI 编程工具实现三屏视觉提供实现基准。所有数值均为准绳（允许 ±2px 抖动），所有颜色以 CSS 变量集中定义，禁止在组件内散落硬编码颜色。
> 三屏代号：now（现在）｜ past（过去）｜ future（未来）。

---

## 0. 设计令牌（Design Tokens）

所有主题以 CSS 变量注入 `:root`，每个页面根节点挂对应主题类 `.theme-now / .theme-past / .theme-future`。亮暗模式切换 `.light/.dark` 覆盖变量。

```css
/* ===== 全局 ===== */
:root {
  --font-sans: "Inter", "HarmonyOS Sans SC", "PingFang SC", "Microsoft YaHei", system-ui, sans-serif;
  --font-serif: "Noto Serif SC", "Source Han Serif SC", "Songti SC", "SimSun", serif;
  --font-mono: "JetBrains Mono", "Fira Code", "IBM Plex Mono", Consolas, monospace;
  --radius-now: 4px;          /* 现在页：方角 */
  --radius-past: 8px;         /* 过去页：适度圆 */
  --radius-future: 0px;       /* 未来页：零圆角 */
  --ease-standard: cubic-bezier(0.25, 1, 0.5, 1);
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
  --dur-fast: .15s; --dur-mid: .3s; --dur-slow: .6s;
  --dur-page: .8s;            /* 三屏切换 */
  --glitch-duration: 1.6s;    /* 未来页故障过渡 */
  --glitch-intensity: .3;     /* 故障强度 0-1 */
}

/* ===== 现在页 now：蓝白（light）/ 蓝黑（dark）扁平 ===== */
.theme-now.light {
  --bg: #F5F8FF; --surface: #FFFFFF; --surface-2: #EDF2FC;
  --text: #0B1B33; --text-dim: #5A6C85; --text-faint: #94A3B8;
  --accent: #2F6BFF; --accent-hover: #1E55E0; --accent-soft: rgba(47,107,255,.10);
  --border: #DCE4F2; --shadow: 0 1px 3px rgba(11,27,51,.06), 0 4px 12px rgba(11,27,51,.05);
  --success: #16A34A; --danger: #DC2626; --warn: #D97706; --secret: #DC2626;
}
.theme-now.dark {
  --bg: #0B1220; --surface: #111B2E; --surface-2: #16233A;
  --text: #E8EEF9; --text-dim: #8FA3C0; --text-faint: #566985;
  --accent: #4D8AFF; --accent-hover: #6B9CFF; --accent-soft: rgba(77,138,255,.16);
  --border: #1F2E47; --shadow: 0 2px 8px rgba(0,0,0,.35);
  --success: #34D399; --danger: #F87171; --warn: #FBBF24; --secret: #F87171;
}

/* ===== 过去页 past：人生编年史（深金史诗调） ===== */
.theme-past.light {
  --bg: #221B12; --surface: #3A2F22; --surface-2: #2E251A;
  --text: #F2E5C9; --text-dim: #C4B8A8; --text-faint: #8A7B66;
  --accent: #D4AF37; --accent-hover: #E6C65C; --accent-soft: rgba(212,175,55,.14);
  --border: rgba(212,175,55,.25); --shadow: 0 2px 10px rgba(0,0,0,.4);
  --gold: #D4AF37; --gold-hi: #FFE9A8; --copper: #B87333;
  --success: #C4A860; --danger: #B87333; --warn: #C4A860; --secret: #B87333;
}
.theme-past.dark {
  --bg: #1A1510; --surface: #2E251A; --surface-2: #3A2F22;
  --text: #E8DCC3; --text-dim: #B3A48C; --text-faint: #7A6B56;
  --accent: #C9A63E; --accent-hover: #DDBE5E; --accent-soft: rgba(201,166,62,.12);
  --border: rgba(201,166,62,.20); --shadow: 0 2px 10px rgba(0,0,0,.5);
  --gold: #C9A63E; --gold-hi: #F0DCA0; --copper: #A0653A;
  --success: #B89B52; --danger: #A0653A; --warn: #B89B52; --secret: #A0653A;
}

/* ===== 未来页 future：赛博朋克（双色主题，默认 青+品红） ===== */
.theme-future.light {
  --bg: #0A0A12; --surface: #12121C; --surface-2: #181826;
  --text: #E0E4F0; --text-dim: #6E7A96; --text-faint: #3E465E;
  --accent: #00FFF5; --accent-2: #FF00FF; --accent-soft: rgba(0,255,245,.10);
  --border: rgba(0,255,245,.22); --glow: 0 0 12px rgba(0,255,245,.45), 0 0 32px rgba(0,255,245,.15);
  --danger: #FF0055; --secret: #FF0055;
}
.theme-future.dark {
  --bg: #07070D; --surface: #0D0D16; --surface-2: #12121E;
  --text: #C9D1E5; --text-dim: #5A6480; --text-faint: #323A52;
  --accent: #00E5FF; --accent-2: #FF00E5; --accent-soft: rgba(0,229,255,.10);
  --border: rgba(0,229,255,.18); --glow: 0 0 10px rgba(0,229,255,.4), 0 0 28px rgba(0,229,255,.12);
  --danger: #FF2A6D; --secret: #FF2A6D;
}

---

## 1. 现在页 Now — 蓝白/蓝黑扁平 · 方角 · 现代

**气质**：干净、高效、信息层级分明；方角为主，圆角 ≤4px；细边框弱阴影；蓝是唯一强调色，红只作"绝密/删除/危险"。

### 1.1 布局（两栏密铺 + 可拖拽分隔）
- 左栏（时间/身体/心情/日记堆叠）与右栏（日历）之间为竖直分隔线，可左右拖拽改宽度（默认 左 45% / 右 55%）。
- 左栏内四块（时间区、身体卡片、心情卡片、日记）间为水平分隔线，可上下拖拽改高度；卡片之间可拖拽交换顺序（身体/心情顺序任意）。
- 实现骨架建议：CSS Grid `grid-template-columns: var(--col-left) 6px var(--col-right)` + 每卡片 `resize: vertical` 不可靠，改用分隔线 drag 更新 grid 模板列/行值。拖拽时给 body 加 `cursor: col-resize`，分隔线 hover 高亮 accent。
- 移动端：单列滚动，卡片顺序=时间→身体→心情→日记→日历。

```css
/* 骨架 */
.screen-now { display: grid; grid-template-columns: 45% 6px 1fr; gap: 0; height: 100vh; }
.screen-now .col-left { display: flex; flex-direction: column; overflow: hidden; }
.screen-now .col-right { overflow: auto; background: var(--surface); }
.divider-v { cursor: col-resize; background: var(--border); transition: background .15s; }
.divider-v:hover, .divider-v.dragging { background: var(--accent); }
```

### 1.2 时间区（左上角）
内容自左向右：**日期大字**（衬线不用，用 700 无衬线 28px）｜ 星期 ｜ 农历（14px dim）｜ 北京时间 hh:mm:ss —— **秒数字体更小**（同一行内秒为 .7em 字号且 opacity .75，用等宽字体防跳动）。

```css
.clock-date { font-size: 28px; font-weight: 700; color: var(--text); font-variant-numeric: tabular-nums; }
.clock-sub { font-size: 14px; color: var(--text-dim); display: flex; gap: 10px; align-items: baseline; }
.clock-seconds { font-size: .7em; font-family: var(--font-mono); opacity: .75; color: var(--text-dim); }
```

### 1.3 卡片通用
```css
.card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-now);
        box-shadow: var(--shadow); padding: 16px 18px; min-height: 0; }
.card-title { font-size: 13px; font-weight: 600; color: var(--text-dim); letter-spacing: .4px;
              display: flex; justify-content: space-between; align-items: center; }
```

### 1.4 身体卡片（左右两半）
左=近 7 日身体曲线（0-100 折线，**不标数值**）；右=添加面板：滑杆 0-100（拖动时显示当前值 tooltip）+ 自由文字备注 + 提交按钮。滑杆为 0-100 渐变底（低→高 由灰蓝渐到 accent）。

### 1.5 心情卡片（左右两半）
左=今日曲线（按分钟时间轴散点连线，数值=所选 tag 均值，不标具体值）；右=添加面板：tag 网格（emoji+名称，多选，选中加 accent 边框与 accent-soft 底）、可选备注、提交。每行最多 6 个 tag。

```css
.tag-grid { display: grid; grid-template-columns: repeat(6, 1fr); gap: 6px; }
.tag-item { display: flex; flex-direction: column; align-items: center; gap: 2px; padding: 6px 2px;
            border: 1px solid var(--border); border-radius: var(--radius-now); cursor: pointer;
            background: var(--surface-2); font-size: 12px; color: var(--text-dim); transition: all .15s; }
.tag-item:hover { border-color: var(--accent); }
.tag-item.selected { border-color: var(--accent); background: var(--accent-soft); color: var(--text); }
.tag-emoji { font-size: 20px; line-height: 1.2; }
```

### 1.6 待办清单
单行 item：方形 checkbox（勾选时打 accent 勾、文字划线变 faint）｜ 文字 ｜ 右侧"..."菜单（编辑日期/权限/删除/里程碑开关）。逾期未完成自动变红点前缀。

### 1.7 按钮体系（现在页）
```css
.btn { border: none; border-radius: var(--radius-now); padding: 8px 16px; font-size: 14px;
       font-weight: 500; cursor: pointer; transition: all .15s; font-family: var(--font-sans); }
.btn-primary { background: var(--accent); color: #fff; }
.btn-primary:hover { background: var(--accent-hover); }
.btn-ghost { background: transparent; border: 1px solid var(--border); color: var(--text-dim); }
.btn-ghost:hover { border-color: var(--accent); color: var(--accent); }
.btn-danger  { background: transparent; border: 1px solid var(--danger); color: var(--danger); }
```

### 1.8 输入框
```css
.input, .textarea { width: 100%; background: var(--surface-2); border: 1px solid var(--border);
  border-radius: var(--radius-now); padding: 8px 12px; color: var(--text); font-size: 14px;
  font-family: var(--font-sans); transition: border-color .15s; }
.input:focus, .textarea:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 2px var(--accent-soft); }
```

### 1.9 绝密模式（全屏主题变红）
- 开启时给根节点加 `.secret-mode`：将 accent/danger 相关色替换为红色系，卡片边框与强调走红，编辑器切换绝密内容层。
```css
.secret-mode.theme-now { --accent: var(--secret); --accent-hover: #B91C1C;
  --accent-soft: rgba(220,38,38,.14); --border: rgba(220,38,38,.35); }
.secret-mode .card { border-color: rgba(220,38,38,.4); }
.secret-mode .clock-date { color: var(--secret); }
.secret-badge { display: inline-flex; align-items: center; gap: 4px; color: var(--secret);
  font-size: 12px; font-weight: 600; letter-spacing: 1px; text-transform: uppercase; }
```

### 1.10 进行中规划横幅轮播（现在页顶部）

- 展示"进行中"规划（判定：开始≤今天≤截止 且 计划进度<100%），横向轮播，间隔可配置（默认 6 秒），可点击暂停/手动切换
- 每张横幅：规划名 + 时间进度条 + 计划进度条 + 起止日期，点击跳转未来页该规划详情
- 风格随现在页（扁平蓝），多张横幅用卡片内轮播条

```css
/* 横幅轮播容器 */
.plan-banner { display: flex; align-items: center; gap: 14px; padding: 10px 16px;
  background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-now);
  box-shadow: var(--shadow); cursor: pointer; }
.plan-banner .banner-title { font-weight: 600; color: var(--text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.plan-banner .banner-progress { flex: 1; }
.carousel-track { display: flex; overflow: hidden; scroll-snap-type: x mandatory; }
.carousel-slide { flex: 0 0 100%; scroll-snap-align: start; }
```
---

## 2. 过去页 Past — 人生编年史 · 深金史诗调 · 庄严·光辉·磨损

**气质**：翻开一部厚重的个人史诗，金色余晖洒在陈年展陈上。庄严，不沉重；光辉，不刺眼。磨损是荣誉的见证——重要节点上的金徽边角有轻微风化划痕，越被反复触摸的标记越有岁月的"包浆"。

### 2.1 背景与质感（四层叠加）

**第一层（底色）**：纯深栗褐 `background: var(--bg)`。
**第二层（暗角）**：`radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,.45) 100%)`，模拟电影镜头暗角，让视线聚焦中轴。
**第三层（胶片颗粒）**：使用 SVG noise 滤镜作为背景图（data URI 或 CSS 自带），极低不透明度 `.03-.05`，制造金粉质感而非数字噪点。
**第四层（金色柔光）**：顶部一道水平渐变 `linear-gradient(180deg, rgba(212,175,55,.08) 0%, transparent 50%)`，有缓慢脉动动画（6s ease-in-out infinite），模拟尘室斜阳。

```css
/* 过去页背景四层 */
.screen-past {
  background: var(--bg);
  position: relative;
}
.screen-past::before {
  content: ''; position: fixed; inset: 0; pointer-events: none; z-index: 0;
  background:
    radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,.45) 100%),
    linear-gradient(180deg, rgba(212,175,55,.07) 0%, transparent 50%);
}
.screen-past::after {
  content: ''; position: fixed; inset: 0; pointer-events: none; z-index: 0;
  opacity: .03;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  background-size: 200px;
}
@keyframes gold-sweep {
  0%, 100% { opacity: .07; }
  50% { opacity: .12; }
}
```

### 2.2 卡片（展柜典藏感）
```css
.card-past { background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius-past); box-shadow: var(--shadow);
  padding: 20px 24px; position: relative; z-index: 1; }
.card-past::before { content: ''; position: absolute; inset: 0; border-radius: inherit;
  pointer-events: none;
  box-shadow: inset 0 0 14px rgba(212,175,55,.04); /* 极浅内发光 */ }
```

### 2.3 磨损感（荣誉的见证）
磨损只出现在**里程碑节点**和**年份标题**上，普通日记卡片保持干净。实现方式：
- 里程碑徽章：在 `::after` 叠加一道极细的 `linear-gradient` 重影，制造划痕感；金边饱和度轻微不均（`mix-blend-mode: multiply` 加杂色贴图）
- 章节年份数字：`text-shadow` 加 1px 偏移的暗边（模拟石刻风化），笔画重影使用 `::before` 错位 0.5px

```css
/* 里程碑徽章磨损 */
.milestone-badge { position: relative; display: inline-flex; align-items: center; gap: 6px;
  padding: 4px 14px; border: 1px solid var(--gold); border-radius: 20px;
  color: var(--gold); font-size: 13px; font-weight: 600; letter-spacing: .5px; }
.milestone-badge::after { content: ''; position: absolute; inset: 0; border-radius: 20px;
  opacity: .12; pointer-events: none;
  background: repeating-linear-gradient(45deg, transparent, transparent 2px,
    rgba(0,0,0,.06) 2px, rgba(0,0,0,.06) 3px); /* 细微划痕纹理 */ }
/* 章节年份风化 */
.chapter-year { font-family: var(--font-serif); font-size: 48px; font-weight: 700;
  color: var(--gold); letter-spacing: 6px; line-height: 1.1;
  text-shadow: 1px 1px 0 rgba(0,0,0,.3), -0.5px -0.5px 0 var(--gold-hi); }
.chapter-year::before { content: attr(data-year); position: absolute;
  left: .5px; top: .5px; color: rgba(0,0,0,.15); pointer-events: none; /* 石刻风化重影 */ }
```

### 2.4 可缩放人生时间轴（2026-09-03 确认，替代旧中轴方案）

**模型**：时间轴 = 一张"可缩放图片"（人生长卷）。范围只覆盖 **注册日 → 今天**，不含更早与未来。竖向轴默认在左侧，可拖放到上/下/右边缘（书写者专属布局，可保存）。

**时间轴方向**：竖置时 **今天在底部**、注册日在顶部，默认滚动到底部（显示今天）；横置时 **今天在右端**、注册日在左端，默认滚动到右端。

**交互**：
- 滚轮：向上=放大（刻度变细，天级），向下=缩小（刻度变粗，月→年级）。放到最大时屏幕内始终显示 **3~5 天**。
- 左键按住拖动=平移（滚动）。
- 点击轴上任意位置 = 精确选中那一天（即使该刻度未显示，月/年级粒度下同样点中具体某天）。
- 刻度过标签：天级标"xx日"（跨月当日带月份，如"9月30日"）；月级标月份；年级标年份。
- 里程碑在轴上特殊标注（金色勋章节点）。

**实现要点**：时间轴本质是一个按日期映射的滚动区域。日期→像素位置线性映射；缩放=改变每像素对应天数（px/天）。放大上限=屏幕内 3~5 天，缩小下限=注册日→今天全部在一屏。

```css
/* 时间轴容器（竖置，今天在底部，默认滚动到底部） */
.timeline-axis { position: relative; width: 72px; background: var(--surface-2);
  border-right: 1px solid var(--border); overflow: hidden; cursor: grab; }
.timeline-axis.dragging { cursor: grabbing; }
.timeline-axis .track { position: absolute; inset: 0; }
/* 刻度线 */
.tick { position: absolute; right: 0; left: 0; height: 1px; background: var(--border); }
.tick-label { position: absolute; right: 6px; font-size: 10px; color: var(--text-dim);
  transform: translateY(-50%); font-family: var(--font-mono); }
/* 里程碑节点（金色勋章） */
.tick-milestone { position: absolute; left: 50%; transform: translate(-50%,-50%);
  width: 16px; height: 16px; background: var(--gold); border-radius: 50%;
  box-shadow: 0 0 8px rgba(212,175,55,.4), inset 0 0 3px rgba(0,0,0,.3);
  z-index: 2; }
/* 今日标记 */
.tick-today { position: absolute; right: 0; left: 0; height: 2px; background: var(--gold-hi);
  box-shadow: 0 0 6px var(--gold); z-index: 2; }
/* 选中日高亮 */
.tick-selected { position: absolute; right: 0; left: 0; height: 100%;
  background: var(--accent-soft); z-index: 1; }
```

**与曲线联动**：两个折线图组件展示当前屏幕时间范围内的趋势（默认 3~5 天），缩放/平移时范围随动、曲线重绘。

### 2.5 里程碑丰碑（放大式节点）
重大里程碑在选中日内容区呈现为"丰碑"——卡片撑满该日内容宽度，背景金辉，标题数字大字号，带磨损徽章（见 2.3）；时间轴上对应位置为金色勋章节点（见 2.4）。普通日记保持标准卡片大小。

```css
.milestone-marker { display: flex; align-items: flex-start; gap: 14px; }
.milestone-icon { font-size: 24px; line-height: 1; opacity: .85; }
```

### 2.6 曲线组件（两个独立折线统计图）

**心情曲线** 与 **身体指数曲线** 是两个独立的折线统计图组件，并排展示（或上下排），随时间轴缩放/平移联动，显示当前屏幕时间范围（默认 3~5 天）的趋势，不标具体数值、只表现势。数据点密度高时自动连成平滑趋势线。

```css
.chart-past { --chart-line: var(--gold); --chart-fill: rgba(212,175,55,.08);
  --chart-point: var(--gold); --chart-hi: var(--gold-hi); --chart-lo: var(--copper); }
/* 折线图容器（两个组件平铺） */
.curves-panel { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
```

### 2.7 主区域布局（时间轴 + 内容）

时间轴（默认左侧、可拖放）外，主区域包含：
1. 顶部：两个曲线组件（心情 + 身体）
2. 中部：选中日期的内容——**日记 与 该日日程（任务）左右对半开**（2026-09-03 确认）
   - 左半：日记卡片（含绝密🔒条目、里程碑徽章、评论区）
   - 右半：该日日程/任务列表（含评论）

```css
.screen-past { display: flex; }
.past-main { flex: 1; overflow: auto; padding: 24px 32px; }
.day-split { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; } /* 日记|日程 对半 */
```

### 2.8 注释系统
评论卡片保持过去风格，鎏金细线边框，字体稍小，与主内容区分。

### 2.9 失联/停止横幅（过去页不变灰）
横幅只显示在现在/未来页顶部，暖黄色底棕文字；过去页始终保留底色。

---

## 3. 未来页 Future — 赛博朋克 · 双色霓虹 · 平行四边形 · 玻璃拟态

**气质**：冷峻、前卫、信息密集。黑暗底色上霓虹发光，所有面板是平行四边形（竖边 75° 倾斜），零圆角，扫描线/CRT 噪点覆盖，发光代替阴影。拒绝"整齐的 dashboard 网格"——元素围绕视觉重心倾斜排布、偏移轴线、部分重叠，参考游戏 HUD 而非管理后台。

### 3.1 平行四边形面板（75° 竖边，两套方案）

**方案 A（推荐，简单可靠）**：容器 skewX 倾斜 + 内部反 skew 补偿。内容保持水平，边框和背景形成平行四边形。

```css
/* 平行四边形面板容器 */
.panel-cyber { transform: skewX(-15deg); /* 竖边与水平夹角 = 90-15 = 75° */
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 0; /* 零圆角 */ position: relative; overflow: hidden; }
.panel-cyber-inner { transform: skewX(15deg); /* 内容补偿回正 */ padding: 18px 22px; }

/* 组合式：面板组斜向排列，模拟游戏 HUD 偏移轴线 */
.panel-group { display: flex; flex-direction: column; gap: 12px;
  transform: rotate(-.5deg); /* 极轻微整体旋转，打破完全对齐 */ }
```

**方案 B（精确 clip-path，适合不倾斜边框场景）**：

```css
.panel-cyber-clip { position: relative; padding: 18px 22px;
  background: var(--surface); border-radius: 0;
  --slant: 22px; /* 斜切量 = H * tan(15°)，H≈80px 时取 22px */
  clip-path: polygon(0 0, calc(100% - var(--slant)) 0, 100% 100%, var(--slant) 100%); }
/* 这种方案用 clip-path 切掉右上角和左下角，形成平行四边形，内容不变形 */
```

### 3.2 HUD 方括号角标（L 形，非连续边框）

每块面板的四角用 L 形角标框定，中间边不画线。使用四个伪元素或 `background` 的 `conic-gradient` 技巧：

```css
/* 四角 L 形角标 —— 使用 conic-gradient 切出四个角 */
.panel-cyber-bracket { position: relative; }
.panel-cyber-bracket::before { content: ''; position: absolute; inset: 0;
  pointer-events: none; z-index: 1;
  background: conic-gradient(from 90deg at 1px 1px, var(--accent) 90deg, transparent 0)
              0 0 / 12px 12px,  /* 左上（使用 12px 角标长度） */
              conic-gradient(from 180deg at calc(100% - 1px) 1px, var(--accent) 90deg, transparent 0)
              100% 0 / 12px 12px,  /* 右上 */
              conic-gradient(from 0deg at 1px calc(100% - 1px), var(--accent) 90deg, transparent 0)
              0 100% / 12px 12px,  /* 左下 */
              conic-gradient(from 270deg at calc(100% - 1px) calc(100% - 1px), var(--accent) 90deg, transparent 0)
              100% 100% / 12px 12px;  /* 右下 */
  background-repeat: no-repeat; opacity: .6;
}
/* 注意：若与 skew 方案叠加，需在 skew 容器上同时处理 */
```

### 3.3 扫描线叠加层（全局）
```css
/* 未来页全局扫描线（不拦截点击） */
.scanlines { position: fixed; inset: 0; pointer-events: none; z-index: 9999;
  background: repeating-linear-gradient(0deg,
    transparent 0px, transparent 2px,
    rgba(0,255,245,.03) 2px, rgba(0,255,245,.03) 3px); }
```

### 3.4 网格背景
```css
/* 赛博朋克网格背景 */
.grid-bg { background-image:
  linear-gradient(rgba(0,255,245,.04) 1px, transparent 1px),
  linear-gradient(90deg, rgba(0,255,245,.04) 1px, transparent 1px);
  background-size: 30px 30px; }
```

### 3.5 霓虹发光（代替阴影）
```css
/* 发光代替阴影表达层级 */
.glow-cyan { box-shadow: 0 0 8px rgba(0,255,245,.35), 0 0 24px rgba(0,255,245,.1); }
.glow-magenta { box-shadow: 0 0 8px rgba(255,0,255,.4), 0 0 24px rgba(255,0,255,.12); }
.glow-accent { box-shadow: 0 0 10px var(--accent), 0 0 32px var(--accent-soft); }
/* focus 状态用发光环 */
.cyber-input:focus { outline: none; box-shadow: 0 0 0 2px var(--accent),
  0 0 12px var(--accent-soft); }
```

### 3.6 玻璃拟态面板
```css
.glass-card { background: rgba(18,18,28,.65); border: 1px solid var(--border);
  backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
  border-radius: 0; }
```

### 3.7 按钮体系（未来页，零圆角，发光 hover）
```css
.btn-cyber { border: 1px solid var(--accent); background: transparent; color: var(--accent);
  padding: 10px 24px; font-family: var(--font-mono); font-size: 13px; font-weight: 500;
  letter-spacing: 1px; text-transform: uppercase; border-radius: 0; cursor: pointer;
  transition: all .2s; }
.btn-cyber:hover { background: var(--accent-soft); box-shadow: 0 0 12px var(--accent-soft); }
.btn-cyber-primary { background: var(--accent); color: var(--bg); }
.btn-cyber-primary:hover { background: var(--accent); box-shadow: 0 0 18px var(--accent); }
```

### 3.8 字体栈（未来页）
```css
.theme-future { --font-ui: var(--font-mono); /* 全 UI 等宽化 */
  --font-heading: var(--font-mono); /* 标题全大写宽字距 */ }
.cyber-heading { font-family: var(--font-mono); font-size: 14px; font-weight: 500;
  letter-spacing: 2px; text-transform: uppercase; color: var(--accent); }
.cyber-data { font-family: var(--font-mono); font-variant-numeric: tabular-nums;
  color: var(--text); }
```

### 3.9 Glitch 故障效果（RGB 分离 + 切片）
用于页面切换过渡和关键高亮（标题/里程碑节点），**禁止全篇滥用**。

```css
/* 三色 RGB 分离 glitch */
.glitch-text { position: relative; color: var(--text); font-weight: 700; }
.glitch-text::before, .glitch-text::after {
  content: attr(data-text); position: absolute; top: 0; left: 0;
  width: 100%; height: 100%; pointer-events: none; }
.glitch-text::before { color: #FF0080; /* 红偏移 */
  transform: translate(-2px, 0); mix-blend-mode: screen;
  animation: glitch-1 var(--glitch-duration) infinite steps(1); }
.glitch-text::after { color: #00FFF0; /* 青偏移 */
  transform: translate(2px, 0); mix-blend-mode: screen;
  animation: glitch-2 var(--glitch-duration) infinite steps(1); }

@keyframes glitch-1 { 0%,100% { transform: translate(-2px, 0); }
  20% { transform: translate(-3px, 1px); }
  40% { transform: translate(-1px, -1px); }
  60% { transform: translate(-2px, 2px); }
  80% { transform: translate(-3px, -1px); } }
@keyframes glitch-2 { 0%,100% { transform: translate(2px, 0); }
  20% { transform: translate(3px, -1px); }
  40% { transform: translate(1px, 1px); }
  60% { transform: translate(2px, -2px); }
  80% { transform: translate(3px, 1px); } }

/* 切片故障（额外层） */
.glitch-slice::before { animation: glitch-slice 1.6s infinite steps(1); }
@keyframes glitch-slice {
  0%, 100% { clip-path: inset(0 0 0 0); }
  15% { clip-path: inset(20% 0 60% 0); transform: translate(-3px, 0); }
  30% { clip-path: inset(50% 0 30% 0); transform: translate(3px, 0); }
  45% { clip-path: inset(40% 0 50% 0); transform: translate(-2px, 0); }
}

/* 无障碍：prefers-reduced-motion 降级 */
@media (prefers-reduced-motion: reduce) {
  .glitch-text::before, .glitch-text::after { animation: none; }
}
```

### 3.10 现在页右栏日历（FullCalendar 风格化）
现在页右栏用 FullCalendar 展示今日/本周任务（未来页的缩放日历见 3.14，为自研组件）。通过 CSS 变量覆盖其默认样式使其融入对应主题：

```css
/* FullCalendar 赛博朋克主题 */
.fc { font-family: var(--font-mono); }
.fc .fc-toolbar-title { font-family: var(--font-mono); font-size: 16px; letter-spacing: 2px;
  text-transform: uppercase; color: var(--accent); }
.fc .fc-button { border: 1px solid var(--border); background: transparent; color: var(--text);
  border-radius: 0; font-family: var(--font-mono); letter-spacing: 1px; }
.fc .fc-button:hover { background: var(--accent-soft); border-color: var(--accent); }
.fc .fc-daygrid-day { border-color: var(--border); }
.fc .fc-day-today { background: var(--accent-soft) !important; }
.fc .fc-event { background: var(--accent-soft); border: 1px solid var(--accent);
  color: var(--text); border-radius: 0; font-size: 12px; }
.fc .fc-event.overdue { border-color: var(--danger); }
.fc .fc-day-past { opacity: .5; }
.fc .fc-col-header-cell { border-color: var(--border); color: var(--text-dim); }
.fc * { border-color: var(--border) !important; }
/* 日历背景带网格 */
.fc .fc-daygrid-body { background-image:
  linear-gradient(rgba(0,255,245,.03) 1px, transparent 1px),
  linear-gradient(90deg, rgba(0,255,245,.03) 1px, transparent 1px);
  background-size: 30px 30px; }
```

### 3.11 滑动条/阶段目标卡片
```css
.goal-card { padding: 14px 18px; border: 1px solid var(--border);
  border-radius: 0; background: var(--surface); display: flex; flex-direction: column; gap: 8px; }
.goal-card .goal-title { font-family: var(--font-mono); font-size: 12px;
  letter-spacing: 1.5px; text-transform: uppercase; color: var(--accent); }
.goal-card .goal-body { color: var(--text-dim); font-size: 13px; line-height: 1.5; }
```

### 3.12 长期规划卡片（未来页上部）

**数据**：名称、开始日期、截止日期、简介、封面图、图标、文件列表、进度记录（日期→百分比）。

**卡片呈现（左 30% 列表 / 右 70% 详情）**：
- 列表项：封面缩略图 + 名称 + 时间进度条 + 计划进度条 + 起止日期
- 时间进度 = (今天-开始)/(截止-开始)；计划进度 = 最近一次标记的 percent
- 双进度条上下排列，时间条用 accent（青），计划条用 accent-2（品红）
- 点击列表项 → 右侧详情：封面大图、图标、简介、起止、文件列表（可下载）、进度历史、进度标记按钮（选日期+百分比）

规划列表按截止日期升序排列。判定"进行中"：开始≤今天≤截止 且 计划进度<100%。

```css
.plan-card { border: 1px solid var(--border); border-radius: 0; background: var(--surface);
  padding: 14px; display: flex; gap: 12px; cursor: pointer; transition: border-color .2s; }
.plan-card:hover { border-color: var(--accent); box-shadow: 0 0 10px var(--accent-soft); }
.plan-card.active { border-color: var(--accent); }
.plan-thumb { width: 72px; height: 72px; object-fit: cover; border: 1px solid var(--border); }
.progress-time { height: 4px; background: var(--accent-soft); }
.progress-time > span { display: block; height: 100%; background: var(--accent);
  box-shadow: 0 0 8px var(--accent); }
.progress-plan { height: 4px; background: rgba(255,0,255,.12); }
.progress-plan > span { display: block; height: 100%; background: var(--accent-2);
  box-shadow: 0 0 8px var(--accent-2); }
```

### 3.13 未来页整体布局（游戏卡片风格）

上下两部分（分隔线可拖拽调整比例，2026-09-03 确认）：
- 上部：左右 3:7，分隔线可拖拽。左=规划列表（按截止日期升序，纵向滚动），右=规划详情
- 下部：可缩放待办日历（自研），默认接下来 7 天，滚轮向下收缩 14/30/半年/1年/5年/10年/30年

```css
.screen-future { display: flex; flex-direction: column; }
.future-top { display: grid; grid-template-columns: 30% 6px 70%; min-height: 40%; }
.future-bottom { flex: 1; display: flex; flex-direction: column; }
```

### 3.14 可缩放待办日历（自研，替代 FullCalendar）

- 横向时间条：默认接下来 7 天；滚轮向下（缩小）= 拉长范围：14 天 → 30 天 → 半年 → 1年 → 5年 → 10年 → 30年 → 100年（最小 7 天，最大 100 年）；向上恢复
- 列=天（窄范围）或 月/季/年（宽范围），事件方块按日期落位
- 点击某天单元格 → 新建该日待办；点击事件 → 编辑（优先级/完成/删除，逻辑沿用现成日历软件）
- 过去日灰显、逾期任务红点
- FullCalendar 可继续用于现在页右栏（今日/本周），未来页此视图自研

```css
.cal-zoom { display: flex; overflow-x: auto; }
.cal-col { flex: 1; min-width: 28px; border-right: 1px solid var(--border); position: relative; }
.cal-col.today { background: var(--accent-soft); }
.cal-event { position: absolute; left: 2px; right: 2px; font-size: 11px;
  background: var(--accent-soft); border-left: 2px solid var(--accent); padding: 2px 4px;
  font-family: var(--font-mono); }
.cal-event.done { opacity: .4; text-decoration: line-through; }
.cal-event.overdue { border-left-color: var(--danger); }
```

---

## 4. 通用组件 · 三屏变体对照表

| 组件 | 现在页 | 过去页 | 未来页 |
|------|--------|--------|--------|
| 圆角 | --radius-now: 4px | --radius-past: 8px | --radius-future: 0px |
| 卡片边框 | 1px solid var(--border) | 1px solid var(--border)+极浅内发光 | 1px solid var(--border)+L 角标 |
| 卡片阴影 | box-shadow（常规） | box-shadow（深）+ inner 金辉 | 不发 shadow，用 glow |
| 背景纹理 | 纯色平 | 胶片颗粒+暗角+金辉 | 扫描线+网格+噪声 |
| 按钮 | 圆角 4px，扁平 | 圆角 8px，细金线 | 零圆角+发光 hover+等宽大写 |
| 字体 | --font-sans | 标题 --font-serif / 正文 --font-sans | --font-mono 全部 |
| 输入框 | 圆角 4px，accent 边框 | 圆角 8px，金边框 | 零圆角+发光 focus |
| 分隔线 | accent 色 hover | 金线 | 霓虹细线 |
| 弹窗 | 扁平方角 shadow | 金边+暗角背景 | 玻璃拟态+发光边框 |
| 切换箭头 | 蓝/灰 | 暗金半透明 | 霓虹半透明 |
| 失联横幅 | 黄褐底棕字（顶部） | 过去页不显横幅 | 黄褐底棕字 |

---

## 5. 三屏切换动效（View Transitions API）

所有页面过渡走浏览器原生 View Transitions API，零额外 JS 动画库开销。在 SPA 路由切换时调用。

### 5.1 触发方式

```javascript
// 切换三屏时（screenNow / screenPast / screenFuture 为路由函数）
function navigateTo(screen) {
  if (!document.startViewTransition) { screen(); return; }
  document.startViewTransition(() => screen());
}
```

### 5.2 现在→未来（赛道切换：故障消失 + 霓虹淡入）

```css
/* 未来页进入：新屏从右侧霓虹光扫入 */
.theme-future::view-transition-new(root) {
  animation: cyberpunk-enter var(--dur-page) var(--ease-out);
}
@keyframes cyberpunk-enter {
  from { opacity: 0; clip-path: polygon(0 0, 100% 0, 100% 0, 0 0); /* 横向扫入 */ }
  to   { opacity: 1; clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%); }
}
/* 现在屏退出：故障切片消失 */
.theme-now::view-transition-old(root) {
  animation: glitch-out var(--glitch-duration) var(--ease-in-out);
}
@keyframes glitch-out {
  0%   { opacity: 1; filter: none; clip-path: inset(0); }
  30%  { opacity: .9; filter: hue-rotate(5deg); clip-path: inset(30% 0 50% 0); transform: translate(-4px, 0); }
  60%  { opacity: .5; filter: hue-rotate(-5deg); clip-path: inset(10% 0 70% 0); transform: translate(3px, 0); }
  100% { opacity: 0; filter: blur(2px); clip-path: inset(0 0 100% 0); }
}
```

### 5.3 现在→过去（暖色光晕过渡）

```css
.theme-past::view-transition-new(root) {
  animation: past-enter 1s var(--ease-out);
}
@keyframes past-enter {
  from { opacity: 0; filter: sepia(.3) hue-rotate(-10deg); }
  to   { opacity: 1; filter: none; }
}
.theme-now::view-transition-old(root) {
  animation: past-out .6s var(--ease-in-out);
}
@keyframes past-out {
  to { opacity: 0; filter: sepia(.4) saturate(.6) brightness(.8); }
}
```

### 5.4 未来→过去/现在（反向，glitch 反向）

```css
.theme-future::view-transition-old(root) {
  animation: glitch-out .8s var(--ease-in-out);
}
.theme-past::view-transition-new(root),
.theme-now::view-transition-new(root) {
  animation: fade-in .5s var(--ease-out);
}
@keyframes fade-in { from { opacity: 0; } to { opacity: 1; } }
```

### 5.5 无障碍降级

```css
@media (prefers-reduced-motion: reduce) {
  ::view-transition-old(root),
  ::view-transition-new(root) { animation-duration: .01ms !important; }
  .glitch-text::before, .glitch-text::after { animation: none; }
}
```

---

## 6. 背景自定义机制

每个页面的背景（单图/多图轮播/纯色/渐变）由用户全权配置，存入 `settings.json`，运行时以 CSS 变量注入。

### 6.1 settings.json 结构

```json
{
  "pages": {
    "now": {
      "bg_type": "solid",        // solid | gradient | image | carousel
      "bg_color_light": "#F5F8FF",
      "bg_color_dark": "#0B1220",
      "bg_image": "",            // upload url
      "carousel": [],            // [{url, interval: 10}]
      "overlay_opacity": 0.85
    },
    "past": {
      "bg_type": "solid",
      "bg_color_light": "#221B12",
      "bg_color_dark": "#1A1510",
      "bg_image": "",
      "carousel": [],
      "overlay_opacity": 0.9
    },
    "future": {
      "bg_type": "solid",
      "bg_color_light": "#0A0A12",
      "bg_color_dark": "#07070D",
      "bg_image": "",
      "carousel": [],
      "overlay_opacity": 0.88
    }
  }
}
```

### 6.2 CSS 变量注入方式

前端加载时，将用户配置动态写入 `:root` 的 CSS 变量：

```javascript
// 注入背景变量
function applyBgTheme(page, config) {
  const root = document.documentElement;
  root.style.setProperty(`--bg-${page}-light`, config.bg_color_light);
  root.style.setProperty(`--bg-${page}-dark`, config.bg_color_dark);
  if (config.bg_image) {
    root.style.setProperty(`--bg-${page}-img`, `url(${config.bg_image})`);
    root.style.setProperty(`--bg-${page}-overlay`, config.overlay_opacity);
  }
}
```

```css
/* 背景图叠在底色之上 */
.screen-past { background-color: var(--bg-past-var);
  background-image: var(--bg-past-img) var(--bg-past-overlay-style, none); }
```

### 6.3 轮播实现

多图轮播用 CSS animation + 多背景图推拉，或 JS 定时切换 background-image（配置 `carousel` 数组每项含 url 和间隔秒数，默认 6s）。切换时带 1.5s cross-fade。

---

## 7. 附件：安全限制（头像/日记附件上传）

| 项目 | 限制 |
|------|------|
| 头像格式 | JPEG / PNG / WebP |
| 头像尺寸 | 最大 512×512 px |
| 头像大小 | 前端限制 2MB，服务器二次校验 |
| 日记附件格式 | 图片 JPEG/PNG/WebP/GIF；文件 PDF/DOC/MP3/MP4 等 |
| 附件大小 | 单文件最大 20MB |
| 后端处理 | 校验 MIME 类型（检查文件头魔数）、重命名防路径穿越 |
| 头像压缩 | Go nfnt/resize 库，等比例缩放到 512px 以内，80% JPEG 质量 |
